# JCIIOT2026 实验开发日志

> 最终状态更新：2026-08-15
> 用途：记录为完成比赛任务而进行的关键实验、结论、证据与提交状态。  
> 原始逐次记录已完整备份到 `JCIIOT/team_submission/audits/实验开发日志_重构前原始记录_20260809.md`，SHA-256 为 `1C049B55C8923BE582F5634D925CDD859DD45D4D7BAC9695D6635E2FB372ED24`。本日志保留关键过程，原始备份保留全部试错细节。  
> 本文件后文包含历史阶段数据；若历史结论与本节冲突，以本节、`team_submission/submission_manifest.json` 和 `team_submission/evidence/EVIDENCE_INDEX.json` 为准。

## 1. 最终提交结论

1. L1～L5 均已有正式满分评分证据，客观分合计 **100/100**。
2. 最终五题证据来自 2026-08-11 联测：L1 39.584 s、L2 39.549 s、L3 40.750 s、L4 49.415 s、L5 98.462 s，总耗时 267.760 s。
3. 主办方维护范围按固定提交 `80d8f9216b0716c6c7a20c19582b532ff1c9cdf2` 审计：4,966 个普通文件一致、6 个 LFS 对象正确实体化、修改 0、缺失 0、违规 0。
4. 五份比赛 SOP 均为团队从对应 Word 任务文档生成的版本，坐标由匹配场景的语义地图取得，不从 Word 图片猜坐标。
5. Execute 创建 Agent 时会根据实际环境自动激活对应的团队 SOP，不再依赖人工记住切换 L1～L5。
6. 当前方案具有可陈述的集成创新：受约束 SOP 编译、程序化教师与 BC 恢复的混合抓取、携物风险感知导航/放置、全过程可审计。它不是新基础网络或新理论，创新表述必须保持诚实。
7. 主办方禁止修改的源码已通过固定基准审计；2026-08-14 已把 MuJoCo 和具体 backend 实现访问从 skill 层迁入允许修改的 workflow 后端适配器。自定义 EnvBackend 适配器的最终接口解释仍以主办方为准。
8. 2026-08-15 使用重构后代码逐题执行 L1–L5 的 `move → pick_up → move → place_down` 离线物理回归，5/5 级别通过，共 7 条完整四技能链路；L5 按后→中→前顺序完成 3/3 个箱子、12/12 步。该报告与 2026-08-11 的历史 100/100 官方评分证据严格分开。

## 2. 主办方规则与修改边界

### 2.1 任务链与评分

标准任务链：

`Word/SOP → LLM 规划 → move → pick_up → move → place_down → trajectory/result/score JSON`

性能评分要点：

- 五个场景满分依次为 10、15、20、25、30 分。
- 成功离开占该题 50%：物体在 x 或 y 方向相对初始位置移动超过 1 m。
- 成功到达占该题 50%：物体平面位置距目标桌面中心小于 0.8 m。
- 任一碰撞会额外扣 5 分。
- 同分时耗时更短者更优。
- 总评中性能占 60%，创新占 40%。100/100 客观分不等于创新分自动满分。

### 2.2 不允许修改的主办方维护范围

- `robosuite/`：仿真环境、机器人、物体模型和控制器。
- `generated_maps/`：预生成语义地图与占据栅格。
- `environments/base.py`、`robosuite_backend.py`：后端协议与仿真实现。
- `core/types.py`：`SkillResult`、`ExecutionContext` 等基础结构。
- `app.py`、`task_config.json`：页面入口和官方 L1～L5 任务配置。

允许且实际使用的优化范围：

- `src/robot_agent/skills/*.py`：导航、抓取、放置、SOP 读取与任务映射。
- `knowledge/robot_params.json`：执行参数与模型路径。
- `team_submission/**`：自生成 SOP、训练数据、配置、模型、封存包、审计和说明文档。

### 2.3 受保护源码审计

- 主办方仓库：`https://github.com/JCIIOT2026/JCIIOT2026`
- 固定提交：`80d8f9216b0716c6c7a20c19582b532ff1c9cdf2`
- 官方 ZIP SHA-256：`d5245cb57b0c9c99253f397950a30a4255e98af78390cc2d0850784cb84bda2a`
- 受保护参考文件：4,972
- 普通文件逐字节一致：4,966
- Git LFS 正确实体化：6
- 修改：0
- 缺失：0
- 违规新增源码：0

六个非逐字节项是官方 baseline、示例 HDF5 和四个桌面网格 ZIP；实体文件大小与 SHA-256 均和固定提交的 LFS 指针一致，不是源码篡改。

受保护目录中另有 Python 缓存、采集演示和安装元数据。这些不是参考源码修改，但正式打包必须排除。

审计命令：

```powershell
conda activate jci_clean
Set-Location D:\project\JCIIOT2026-master\JCIIOT2026-master\JCIIOT
python .\team_submission\audits\verify_official_boundary.py
```

输出：

- `team_submission/audits/official_boundary_audit.md`
- `team_submission/audits/official_boundary_audit.json`

## 3. 整体技术链

### 3.1 SOP 与坐标是怎样得到的

必须区分“任务语义”和“世界坐标”：

1. Word 文档给出材料、人工工位名称、数量和操作要求。
2. 锁定的 `task_config.json` 把当前题映射为 level、环境、source、target 和精确 object_name。
3. `read_document.py` 根据环境选择对应 semantic map。
4. semantic map 提供站点中心、approach、障碍物和占据栅格等场景信息。
5. 代码把 Word 任务语义与地图坐标合并，生成团队 `generated_sop_l1.md`～`generated_sop_l5.md`。
6. 生成文件进入 `team_submission/knowledge/current_generated_sop.md`，随官方基础知识一同注入规划器。
7. `skills/library.py` 在 Execute 时读取实际 backend 环境，自动激活对应关卡的团队 SOP，并校验 level、environment、source、target 标记。

因此，SOP 中的坐标不是 Word 直接提供的，也不是从图片估算的，更不是在 skill 中手写固定答案；它们来自该题对应的官方语义地图。

当前审计状态：

| 关卡 | Word | 环境 | source | target | 团队 SOP |
|---|---|---|---|---|---|
| L1 | case 1 | `FactorySorting1_3FO3ERFHISEM` | `input_5` | `output_4` | 通过 |
| L2 | case 3 | `FactorySorting3_3FO3ERRPH7X9` | `input_6` | `output_4` | 通过 |
| L3 | case 5 | `FactorySorting5_3FO3ERTPXEUT` | `aux_input_1` | `output_5` | 通过 |
| L4 | case 7 | `FactorySorting7_3FO3ERFKY9RN` | `input_2` | `output_5` | 通过 |
| L5 | case 9 | `FactorySorting9_3FO3ERT2C5FP` | `input_1` | `aux_output_1` | 通过 |

验证命令：

```powershell
python .\team_submission\audits\verify_generated_sops.py
```

说明：规划器仍会读取主办方 `knowledge/` 作为平台基础知识；当前任务的团队自生成 SOP 随后从 `team_submission/knowledge/` 注入并作为任务级约束生效。不能错误宣称“完全没有读取官方知识”，正确说法是“实际任务 SOP 为团队生成，官方知识保留为平台基础知识”。

### 3.2 collect、HDF5、训练配置和 checkpoint

一次抓取示范被离散成多个时间步。每个时间步至少保持：

- `obs_t`：动作前的当前观察，可包含关节/手臂位姿、物体状态和图像，具体字段由采集及训练配置决定。
- `action_t`：程序化教师在该观察下给出的 20 维控制动作，动作值按控制器约束缩放。
- 环境元数据与 episode 边界。

闭环顺序是：

`collect 读 obs_t → 教师计算 action_t → MuJoCo 执行动作并更新物理状态 → 得到 obs_(t+1) → 成功 episode 写入 HDF5`

关键结论：

- collect 决定 HDF5 实际记录什么。
- HDF5 可以比训练实际使用的信息更完整。
- 训练配置决定 robomimic 从 HDF5 选择哪些 observation key、图像、序列窗口、batch 和优化参数。
- checkpoint 保存学习后的权重及其 observation/action 契约；在线推理必须提供与训练时一致的输入字段、形状和预处理。
- BC 学到的是 `obs → action` 的近似映射，不是记住单个抓取点，也不会保证每一步与教师完全相同。
- 只保存成功示范会提高标签质量，但也会造成失败状态覆盖不足，因此闭环出现偏差时可能累积漂移。

### 3.3 最终抓取策略

单独依赖未经对齐的 BC 曾出现起始位姿或视觉分布变化后推箱、夹空、抓起后掉落等问题。最终抓取路由按题目配置：

1. 从实时 MuJoCo 物体中心、抓取 site、底盘位姿和接触状态生成双臂程序化动作。
2. 抓取前同步临时抓取环境与在线导航环境的物体状态，避免两个环境看见不同箱子。
3. 以四指接触、双臂持握和抬升结果做物理成功验证。
4. 最终 L1～L5 均采用程序化抓取优先、逐题 robomimic checkpoint 恢复；此前 L1 的 BC 优先试验仅为历史阶段。
5. L1 BC 输出只施加来自示范分布的通用执行约束：双臂动作渐变和未示范旋转维度置零，不写死目标轨迹。

这不是“删除模型”。路由可以按已验证证据选择学习策略或程序化策略优先，另一层负责恢复；两条路径都必须通过相同物理成功判据。

### 3.4 最终导航与放置策略

- A* 使用额外障碍膨胀，给双臂和携带物留安全余量。
- 抓取后从实时位置沿 source 外向轴直退，再接入全局路径，避免横移扫到相邻箱。
- 直线离站必须逐点通过原始占据栅格验证；不通则回退 A*。
- 放置前先退到安全区完成转向，再直线靠近桌面，减少携物原地旋转碰撞。
- L5 对三个物体动态分配目标桌面的中心/左右槽位，防止三个箱子重叠。
- 已验证题使用 `max_linear=2.0 m/s`、`max_angular=1.2 rad/s`；速度提高必须以零碰撞满分证据为前提。

## 4. 五题正式成绩

| 关卡 | 满分 | 正式证据时间戳 | 耗时 | 碰撞/安全扣分 | 最终距目标中心 | 状态 |
|---|---:|---|---:|---:|---|---|
| L1 | 10 | 20260811_103852 | 39.584 s | 0 | 0.12 m | 最终联测证据 |
| L2 | 15 | 20260811_103345 | 39.549 s | 0 | 0.15 m | 最终联测证据 |
| L3 | 20 | 20260811_103547 | 40.750 s | 0 | 0.15 m | 最终联测证据 |
| L4 | 25 | 20260811_102416 | 49.415 s | 0 | 0.17 m | 最终联测证据 |
| L5 | 30 | 20260811_102712 | 98.462 s | 0 | 0.09 / 0.55 / 0.10 m | 最终联测证据 |

合计：**100/100**。

相对原始满分版的提速：

| 关卡 | 原始满分耗时 | 优化后耗时 | 缩短 | 降幅 |
|---|---:|---:|---:|---:|
| L2 | 145.324 s | 92.383 s | 52.941 s | 36.43% |
| L3 | 143.065 s | 89.973 s | 53.092 s | 37.11% |
| L4 | 175.071 s | 97.119 s | 77.952 s | 44.53% |
| L5 | 530.684 s | 235.522 s | 295.162 s | 55.62% |

上表是最终提交唯一采用的成绩集。此前 150.099 s、92.383 s、89.973 s、97.119 s、235.522 s 等数据仅属于历史优化阶段，不再作为最终结果。

## 5. 分题关键实验与历史阶段结论

本节保留优化过程中的阶段性耗时，用于说明失败—修正过程；这里标记为“历史阶段结果”的数值不属于最终提交成绩。最终成绩只使用第 4 节的 2026-08-11 联测证据。

### 5.1 L1：团队模型最终联测通过

- 任务：`input_5 → output_4`，物体 `line_5_container_h01_near`。
- 最终结果：10/10、39.584 s、零碰撞，证据位于 `team_submission/evidence/L1/`。
- 历史正式记录使用官方 `robosuite/robosuite/model_epoch_150.pth`，该模型不再作为当前提交候选的运行时 checkpoint，只保留作比较基线。
- 团队重新采集 121 条成功示范：标准站位 30、底盘扰动 60、物体扰动 30、对齐探针 1。所有正式训练数据均关闭彩色抓取 site 标记。
- 首轮失败消融确认：带 site 标记数据与在线无标记图像的平均绝对误差为 4.7888；无标记训练副本与在线首帧逐像素误差为 0。
- 不含时间步的模型会提前闭爪；加入与在线 wrapper 一致的时间步后恢复动作阶段。逐动作维度 min-max 归一化解决小幅机械臂动作被底盘/夹爪大动作压低的问题。
- 最终团队 checkpoint：`team_submission/training_outputs/factory_sorting_l1_nomarker_timestep_minmax_team_bc/20260810015446/models/model_epoch_20.pth`，SHA-256 `1B13ED397D5D52141A9A32610DFC4A8829BDC6F4D5164F97CE9E5BD01F7E251E`。随机初始化训练，没有载入官方 L1 权重。
- 离线种子 12201、12202、12203 及实际 skill 适配路径种子 12301 全部通过：四指接触、左右抓取均为真，箱体净抬升约 0.126939 m 且保持夹持。
- 最终 `robot_params.json` 按环境指向团队模型；运行路由为程序化抓取优先、BC 恢复。L1 已完成前端联测并纳入最终 100/100 证据集。

回退证据：`team_submission/success_baselines/L1_success_20260729_141634/`。

### 5.2 L2：纠正采集站位与在线场景不一致

- 任务：`input_6 → output_4`，物体 `green_tote_b01_upper`。
- 初期错误：用旧采集站位或错误箱体布局训练，训练时机器人看到的箱子与线上停站后的目标分布不一致；BC 即使训练 loss 正常，也会在错误分布上抓取。
- 修正：从在线真实停止位姿和场景对象动态获取目标；验证双臂四指接触和抬升；保留 50 epoch BC 作为恢复。
- 最终：程序化抓取优先、抓后直退 0.60 m、A* 额外余量 0.20 m、直线速度 2.0 m/s。
- 历史阶段结果：15/15、92.383 s、零碰撞；最终联测结果见第 4 节。

回退与最终版：

- `team_submission/success_baselines/L2_success_20260730_130102/`
- `team_submission/optimization_versions/L2_final_optimized_20260808_233349/`

### 5.3 L3：站位、推箱和抓后碰邻箱

- 任务：`aux_input_1 → output_5`，物体 `blue_tote_b01_far_right`。
- 主要失败：
  - 任务映射错误时机器人走到不正确工位。
  - 底盘未正对桌面，双臂前推导致箱体移动。
  - 抓起后直接横移或转向，携物/机械臂扫落相邻箱。
  - 放置目标名称适配不完整时，携物到达但释放失败。
- 修正：
  - 使用锁定任务配置和语义地图校验站点与物体。
  - 从当前物体姿态动态生成程序化抓取。
  - 抓后沿 source 外向轴直退 0.60 m，再进入 A*。
  - 放置层适配语义目标，并先在安全区转正。
  - L3 100 epoch BC 只做恢复。
- 历史阶段结果：20/20、89.973 s、零碰撞；最终联测结果见第 4 节。

回退与最终版：

- `team_submission/success_baselines/L3_success_20260730_220647/`
- `team_submission/optimization_versions/L3_final_optimized_20260808_231428/`

### 5.4 L4：BC 接近不流畅与临时环境生命周期

- 任务：`input_2 → output_5`，物体 `blue_container_h01_back_upper`。
- 原始成功版使用 L4 50 epoch BC，抓取成功但机械臂接近阶段约 42 s，动作不够流畅。
- 优化为程序化抓取优先，BC 仅恢复；程序化运动步数按 `scripted_motion_step_scale=0.75` 缩放，导航速度 2.0 m/s。
- 曾尝试跨子进程缓存官方教师站距，机器人动作可以成功，但评分子进程在写出结果后卡在关闭阶段。该方案已经完全撤回，缓存删除。
- 抓取时偶发短暂 MuJoCo 窗口是临时物理环境创建/关闭的显示现象。继续消除需要绕过主办方后端生命周期，风险大于收益，因此保留。
- 历史阶段结果：25/25、97.119 s、零碰撞；最终联测结果见第 4 节。

回退与最终版：

- `team_submission/success_baselines/L4_success_20260808_133433/`
- `team_submission/optimization_versions/L4_final_optimized_20260808_143635/`

### 5.5 L5：连续搬运三个箱子

- 任务：从 `input_1` 将三个白边储物箱搬到 `aux_output_1`。
- 主要难点：
  - 每个物体需要独立完成 move → pick_up → move → place_down，不能默认一次抓两个。
  - 多次往返放大导航时间和碰撞风险。
  - 第三个箱位受前两个搬运后的场景状态影响。
  - 三次使用同一目标中心会发生重叠或互相撞落。
- 修正：
  - 对每个精确 object_name 重复完整四步流程。
  - 抓取目标、顺序和状态均使用实时环境数据。
  - 为目标桌面动态分配中心/左右槽位，保证三个物体均在 0.8 m 评分半径内且互不重叠。
  - 安全区预转向、抓后直退、速度提高到 2.0 m/s。
  - L5 100 epoch checkpoint 保留为异常恢复。
- 历史阶段结果：30/30、235.522 s、零安全扣分；最终联测结果见第 4 节。

回退与最终版：

- `team_submission/success_baselines/L5_success_20260808_000324/`
- `team_submission/optimization_versions/L5_final_optimized_20260808_130642/`

## 6. 创新分现状

### 6.1 已有可证明的创新贡献

1. **文档—任务—地图三重约束的 SOP 编译**  
   Word 提供任务语义，锁定任务目录提供环境和精确对象，语义地图提供坐标；生成物带来源哈希，并在 Execute 时按环境自动激活。

2. **程序化教师优先、BC 恢复的混合抓取**  
   不是把传统 BC 当作万能端到端策略，而是用实时物理状态构造确定性正常路径，在物理验证失败时调用已训练模型恢复。

3. **面向携物外形的风险感知导航和放置**  
   在底盘占据栅格上增加携物余量、可通行离站门、A* 降级、安全区预转向和多物体槽位。

4. **可审计、可回退、可复现的比赛工程方法**  
   原始成功版与优化版分开封存；每次正式结果有 score/result/trajectory；受保护源码逐文件哈希；SOP 有输入输出哈希。

### 6.2 不能夸大的内容

- A*、BC、提示词和 robomimic 本身不是本项目原创。
- 没有提出新的基础神经网络、损失函数或理论证明。
- 创新点是约束编译、混合执行、闭环可靠性和审计体系的组合与增强。
- 主观评审无法仅靠客观满分保证；必须用消融证据解释每个机制解决了什么失败。

完整创新说明：`新颖性声明.md`。

## 7. 证据与复现

### 7.1 正式评分证据

- L1：`recordings/FactorySorting1_3FO3ERFHISEM/score_20260729_141634_OK.json`
- L2：`recordings/FactorySorting3_3FO3ERRPH7X9/score_20260808_233349_OK.json`
- L3：`recordings/FactorySorting5_3FO3ERTPXEUT/score_20260808_231428_OK.json`
- L4：`recordings/FactorySorting7_3FO3ERFKY9RN/score_20260808_143635_OK.json`
- L5：`recordings/FactorySorting9_3FO3ERT2C5FP/score_20260808_215445_OK.json`

同目录保留对应 `result_*.json`；有轨迹记录的题同时保留 `trajectory_*.json`。

### 7.2 启动前端

```powershell
conda activate jci_clean
Set-Location D:\project\JCIIOT2026-master\JCIIOT2026-master\JCIIOT
.\team_submission\start_frontend.ps1
```

每次切换题目时仍应重新执行 LLM Plan；Execute 会按实际环境自动激活正确的团队 SOP，不再要求手工覆盖 `current_generated_sop.md`。

### 7.3 当前模型与配置

- L1：`team_submission/models/final/l1/model_epoch_20.pth`。
- L2：`team_submission/models/final/l2/model_epoch_50.pth`。
- L3：`team_submission/models/final/l3/model_epoch_100.pth`。
- L4：`team_submission/models/final/l4/model_epoch_50.pth`。
- L5：`team_submission/models/final/l5/model_epoch_100.pth`。

最终执行采用环境/任务驱动配置；五个环境到上述 checkpoint 的映射由 `robot_params.json` 和提交 manifest 同时固定。

## 8. 已知风险与提交前清单

### 8.1 已知风险

- 2026-08-14 合规重构后，skill 层仅依赖 `CompetitionEnvBackend` 能力协议；实时仿真访问统一封装在 workflow 后端适配器内。该扩展仍需接受主办方最终接口解释。
- L5 回归发现先移除中心箱会使后排箱在持续物理步进中失稳；团队 SOP 生成器现按对象名称中的前/中/后语义形成后排优先顺序，不写死世界坐标，也不修改锁定任务目录。
- L4/L5 抓取时可能短暂闪现临时 MuJoCo 窗口；已经验证不影响得分，不再冒险修改后端。
- `team_submission/.local/` 保存本机 API 配置，只能本地使用，绝不能进入提交包或公开仓库。
- `__pycache__`、`.pyc`、`robosuite.egg-info`、`demonstrations_private` 和临时训练/渲染文件必须从正式提交包排除。
- 不要为了隐藏界面、缓存环境或加快退出而修改 `app.py`、backend 或主办方 MuJoCo 生命周期。

### 8.2 正式提交前必须执行

1. 运行 `verify_official_boundary.py`，要求违规为 0。
2. 运行 `verify_generated_sops.py`，要求 5/5 通过且 Execute 自动激活为 True。
3. 检查 `team_submission/.local/` 和所有 API key 未被打包。
4. 用封存包和正式 score/result/trajectory 复核 L1～L5。
5. L1 若要提交当前提速候选，必须重新跑出 10/10、零碰撞、正常退出的新证据；否则恢复 L1 成功基线。
6. 提交材料中加入 `新颖性声明.md`，并用失败—修正—定量结果说明创新贡献。
7. 最后再次确认 `app.py`、`task_config.json`、backend、core、场景、控制器和 generated maps 未修改。

完整合规清单：`提交合规说明.md`。

## 9. 文档索引

- 重构后的主日志：`实验开发日志.md`
- 重构前原始记录：`JCIIOT/team_submission/audits/实验开发日志_重构前原始记录_20260809.md`
- 新颖性声明：`新颖性声明.md`
- 提交合规清单：`提交合规说明.md`
- 主办方源码边界审计：`JCIIOT/team_submission/audits/official_boundary_audit.md`
- 团队生成 SOP 审计：`JCIIOT/team_submission/audits/generated_sop_audit.md`
- 五份自生成 SOP：`JCIIOT/team_submission/generated_sops/`
- 原始成功版：`JCIIOT/team_submission/success_baselines/`
- 优化成功版：`JCIIOT/team_submission/optimization_versions/`
- 训练配置：`JCIIOT/team_submission/training_configs/`
- 训练输出：`JCIIOT/team_submission/training_outputs/`

---

本日志以后只记录对比赛方案有价值的关键变化：问题、原因、修改、验证证据和回退点。重复启动、临时截图、已撤销的小改动不再反复追加；这些历史仍可在重构前原始备份中查询。
