# 新颖性声明（Novelty Statement）

## 声明摘要

本方案的贡献不是声称提出新的基础神经网络，而是在主办方固定的 FactorySorting、EnvBackend、SkillResult 和自动评分协议之上，构建了一套**任务语义驱动、可恢复、携物安全且可审计的分层具身执行架构**。它把 Word SOP、锁定任务目录、语义地图、实时 MuJoCo 状态、程序化教师控制器和行为克隆策略连接成闭环，并在 L1–L5 五个异构场景中获得现有正式记录的 100/100 客观分。

创新类型属于算法与架构集成创新、可靠性增强和可复现工程方法，不把常规 A*、BC 或提示词本身包装成原创理论。

## 1. 文档—任务—地图三重约束的 SOP 编译

主办方 Word 只描述材料、人工工位名称、数量和流程；世界坐标并不来自 Word 图片。本方案将 `read_document.py` 从简单的 DOCX/VLM 读取器扩展为受约束的 SOP 编译器：

1. 从 Word 提取当前任务语义；
2. 用锁定 `task_config.json` 解析 level、环境、source、target 和精确物体名；
3. 用匹配场景的 semantic map 读取工位中心与 approach；
4. 生成团队自有 `generated_sop_l1～l5.md`，不覆盖主办方 SOP；
5. Execute 构建 Agent 时，`skills/library.py` 根据实时环境自动激活对应团队 SOP，并校验 level、environment、source、target 四类绑定标记。

该链路避免“从图片猜坐标”和“把某题坐标写死进 skill”，也解决了五题切换时人工忘记更换当前 SOP 的风险。五份 Word、语义地图和生成 Markdown 均有 SHA-256 清单，可复核生成来源。

## 2. 可配置优先级的程序化教师—行为克隆混合抓取

单纯 BC 在闭环执行中容易因起始位姿、视觉分布或第一步误差发生累积漂移；单纯程序化控制又可能在特殊物体状态下失败。本方案在 `pick_up.py`、`grasp_alignment.py` 和 `grasp_fallback.py` 中实现可按题目证据选择优先级的两层策略：

- 第一层从实时 MuJoCo 物体中心、抓取 site、底盘位姿和接触状态动态生成双臂动作；
- 临时抓取环境与导航环境同步材料物体状态，避免“抓取环境看见的箱子”和线上场景不一致；
- L1～L5 以程序化抓取为正常路径，并按当前官方环境自动选择该题 robomimic checkpoint 作为恢复；
- L1 的 BC 执行器加入从成功示范归纳的动作渐变与零旋转约束，不写死世界坐标或抓取轨迹；
- BC 初始化、在线 observation 契约和失败恢复均被显式保留，不把模型删除或伪装成无模型方案；
- 成功必须通过接触、双臂持握和抬升验证，不以动作执行完毕代替物理成功。

这一结构不固定宣称某一类控制器永远更好，而是用离线物理门和逐题正式证据决定优先级，并让另一层承担恢复。

## 3. 面向携物外形的风险感知导航与放置

官方占据栅格主要描述底盘通行性，而双臂伸展和携带箱体会扩大实际扫掠范围。本方案在 `move.py` 和 `place_down.py` 中增加：

- A* 障碍额外膨胀与端点有限豁免；
- 从实时抓取位计算 source 离站候选，不把公共 approach 当成必经横移点；
- 直线离站必须逐点通过原始占据栅格验证，阻塞时自动回到 A*；
- 狭窄地图在无法维持目标余量时按地图分辨率逐级降级，而不是穿越障碍；
- 放置前先退到安全区完成转向，再直线接近目标，降低携物原地旋转碰撞；
- L5 多物体按目标桌中心、尺寸和接近方向动态分配中心/左右槽位；SOP 编译器再依据物体名称的前/中/后语义采用后排优先调度，避免三个箱体重叠，并避免先移除相邻箱后后排箱失稳。

这些规则都由当前地图和实时状态决定，没有保存 L1–L5 固定世界坐标。

## 4. 受保护边界与实验结果可审计

方案新增只读边界审计器，以原始 GitHub ZIP 为不可变基准，对主办方维护范围逐文件计算 SHA-256。当前结果：

- 受保护参考文件 4,972 个；
- 4,971 个逐字节一致；
- 1 个为 Git LFS 指针正确实体化：`model_epoch_150.pth` 的实际 SHA-256 与指针 OID 完全一致；
- 修改 0、缺失 0、违规新增源码 0；
- 缓存、采集数据和安装元数据被单独标记为“提交时排除”。

每题还保留原始成功回退包、优化后运行包、官方 score/result/trajectory 证据和压缩包哈希，使性能提升可以回溯，不依赖口头描述。

## 5. 定量证据

| 关卡 | 正式得分 | 可核验基线 | 优化后正式时间 | 相对缩短 | 安全结果 |
|---|---:|---:|---:|---:|---|
| L1 | 10/10 | 150.099 s | 39.584 s | 73.63% | 满分 |
| L2 | 15/15 | 145.324 s | 39.549 s | 72.79% | 满分 |
| L3 | 20/20 | 143.065 s | 40.750 s | 71.52% | 满分 |
| L4 | 25/25 | 175.071 s | 49.415 s | 71.77% | 满分 |
| L5 | 30/30 | 530.684 s | 98.462 s | 81.45% | 满分；三箱均到达 |

上表保留 2026-08-11 的历史优化对比。完成 skill/backend 合规边界重构后，2026-08-15 又通过官方 `app.py` 的本地评分链路取得 L1–L5 合计 100/100、268.135 s；新证据位于 `JCIIOT/team_submission/evidence_retest_20260815/`，不覆盖历史记录。

历史 L5 满分证据中三个箱体最终距目标中心分别为 0.09、0.55、0.10 m，均位于 0.8 m 判定半径内。提交版进一步将五个已验证模型收敛为按环境自动路由，并在 2026-08-15 合规边界重构后完成 L1–L5 物理回归（5/5 级别、7 条完整四技能链路通过）；L5 后→中→前完成 3/3 个物体、12/12 步。历史评分文件与重构后回归报告分开保存。

## 6. 关键消融与失败证据

- BC-only：在 L2/L3/L5 出现起点或闭环分布偏移时，可能推箱或掉箱；改为程序化优先、BC 恢复后，L2–L5 正式满分且显著缩短时间。
- L1 图像域：采集时显示彩色抓取 site 会造成训练—在线 RGB 平均绝对误差 4.7888，模型抓取失败；关闭标记后首帧逐像素误差降为 0。
- L1 时序与尺度：无时间步模型提前闭爪；加入在线一致时间步后恢复阶段顺序。未逐维归一化时 5 厘米级手臂动作被底盘和夹爪大动作主导；逐维 min-max 归一化并施加示范一致的动作渐变后，四个独立离线物理门均通过，箱体稳定净抬升约 0.126939 m。
- 公共 approach 离站：L3 会横向扫到邻箱；改为实时抓取位离站后保持满分并降至 89.973 s。
- 无多槽位放置：L5 三箱重叠或后续搬运碰落；动态槽位后获得 30/30。
- 携物原地转向：曾把箱体扫到桌外；安全区预转向后 L4/L5 无安全扣分。
- 不校验直退：L1 语义外向线会穿过阻塞栅格；加入直线段通行验证后自动选择历史满分所用的狭窄 A* 出口。

这些失败记录说明最终结构不是任意堆叠模块，而是由可重复的物理故障逐层推导得到。

## 7. 原创范围与局限

继承自平台的部分包括 MuJoCo/robosuite 环境、机器人与物体模型、控制器、generated maps、EnvBackend、SkillResult/ExecutionContext、A* 基础实现、评分器和任务目录。团队贡献集中在允许范围内的 skill 编排、知识生成、参数、训练/恢复策略和审计工具。

本方案不宣称新的神经网络结构或普适理论最优性。程序化教师依赖场景中可解析的物体几何和抓取 site；BC 仍可能受到训练分布外状态影响。各题 BC 模型是程序化抓取失败时的恢复层；最终记录不能证明其覆盖所有分布外初始状态。

## 8. 可复核入口

- 边界审计：`JCIIOT/team_submission/audits/verify_official_boundary.py`
- SOP 审计：`JCIIOT/team_submission/audits/verify_generated_sops.py`
- 边界报告：`JCIIOT/team_submission/audits/official_boundary_audit.json`
- SOP 报告：`JCIIOT/team_submission/audits/generated_sop_audit.json`
- 最终五题证据：`JCIIOT/team_submission/evidence/`
- 最终模型清单：`JCIIOT/team_submission/models/final/MODEL_MANIFEST.md`
- 完整关键实验记录：项目根目录 `实验开发日志.md`
