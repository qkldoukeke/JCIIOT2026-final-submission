# JCIIOT2026 项目学习日志

> 用途：持续记录项目讲解进度、已经确认的代码事实、当前问题和下一步。  
> 学习方式：每次只讲一个小闭环；先建立整体地图，再逐文件深入。
> 首要目标：服务于完成比赛题目；重点掌握模块定位、允许修改位置、失败分类和验证方法，不追求无关底层细节。

## 学习状态

- 当前阶段：第10课——训练命令、输出目录与 checkpoint
- 已检查日期：2026-07-29
- 项目代码根目录：`JCIIOT/`
- 下一课：第11课——在线 pick_up、BC 与 lift 的完整链路

## 你在开始时的理解

以下内容先原样保留为“学习起点”，后续逐项用代码验证，不急着一次性判断对错：

1. `move` 主要负责导航和路径规划。
2. `pick_up` 的成功率可能主要取决于抓取模型训练是否充分。
3. L1 采集脚本产生 HDF5，成功抓取的数据用于训练模型。
4. 采集脚本可能参照 L1 的 scene JSON 完成抓取。
5. SOP Word 文档被转换成 `knowledge/sop1.md`，再提供给 LLM 生成逻辑流程。
6. 尚不清楚：
   - `collect` 旁边的 `scene.py` 有什么作用；
   - 拿起和放下是否调用同一个模型；
   - SOP 中的行动坐标从哪里来；
   - HDF5、模型、技能、地图和 LLM 之间怎样连接。

---

## 五个比赛题目的难度递进

五道题并不是单纯把导航距离逐渐加长，而是在同一条执行链上逐层增加困难：

```text
理解任务 → 规划步骤 → move 到正确站位 → pick_up → 携物 move → place_down
```

L1 是基准题。它主要验证单个目标、固定取放点和完整四步流程是否连通。难点集中在精确站位、朝向以及基础抓取模型是否与在线观察一致。

L2 开始增加跨区域导航和障碍物。抓取物体的形状与 L1 不同，因此 L1 模型能够走到正确地点，也不代表能抓起 L2 物体。除训练对应物体外，还要避免空载和携物导航发生碰撞。

L3 增加环境干扰。目标旁边可能有其他箱体或设备，抓取和转向的安全空间更小。即使完成运输，也可能因为推倒旁边物体或导航碰撞被扣安全分，因此需要同时优化站位、抓取轨迹和携物路径。

L4 使用更大、可能成组或上下排列的物体。难点包括准确选择指定目标、双臂同步、较高重心、抓取后的稳定抬升，以及携带大物体时更大的碰撞外形。只训练错误层或错误箱体，即使动作本身成功，也不能完成题目。

L5 是多目标连续任务，需要搬运三个不同位置的箱体。它不只是把同一动作重复三次：每搬走一个物体，场景状态都会变化；机器人需要重新规划、重新站位和再次抓取。一个 L5 checkpoint 还必须覆盖 center、front、back 三种观察与相对位置。任意一次失败都可能中断后续流程，长距离、多次运输也放大了用时和碰撞风险。

分值从 L1 到 L5 为 10、15、20、25、30。题目越靠后，完成度、安全性和时间之间的耦合越明显：BC 模型主要解决抓取动作，move 负责导航，SOP 与 LLM 负责选择正确对象、站点及执行顺序，三者不能互相替代。

拿到新题时，优先按以下顺序定位难点：

1. 从题目和 SOP 确认要搬几个物体、名称、source、target 和顺序。
2. 用运行时任务配置与场景检查映射是否一致，不能盲信旧参考坐标。
3. 先验证 move 的终点站位和 yaw。
4. 再对每类目标做少量可视化程序化抓取。
5. 抓取正确后采集均衡数据、训练 BC，并做抓取加抬升离线验证。
6. 最后进行完整在线运输，检查放置、碰撞和用时。

---

# 第 1 课：先把项目看成三条流程

## 1. 最重要的总图

这个项目不是“输入一句话，然后模型直接控制机器人”。它由三条流程组成：

```text
A. 比赛执行（在线）
用户任务
  → LLM 生成技能计划
  → move / pick_up / move / place_down
  → MuJoCo 中执行
  → 保存轨迹
  → app.py 评分

B. 抓取训练（离线）
脚本制造正确抓取示范
  → 只保留成功 episode
  → HDF5 数据集
  → robomimic 训练 BC 模型
  → .pth checkpoint
  → 在线 pick_up 加载 checkpoint

C. SOP 知识生成（离线准备）
SOP .docx
  → 提取文字和图片
  → VLM 理解图片
  → 整理成知识库 Markdown
  → LLM 规划时把知识注入 prompt
```

三条线的会合点是：

- SOP 知识帮助 LLM 决定“做什么、去哪里、调用哪个技能”。
- 地图帮助 `move` 算出“底盘怎么走”。
- BC checkpoint 帮助 `pick_up` 决定“机械臂下一步怎么动”。
- 最终都在比赛执行流程中汇合。

## 2. 在线比赛流程的输入和输出

以 L1 为例，当前 `knowledge/task_config.json` 指定：

- 场景：`FactorySorting1_3FO3ERFHISEM`
- 取货站：`input_5`
- 放货站：`output_4`
- 候选目标物：`line_5_container_h01_near`、`line_5_container_h01_far`

### 第一步：用户任务进入 LLM 规划器

输入：

- 用户自然语言任务；
- 已注册技能名称；
- `knowledge/` 中的 Markdown 知识；
- 当前任务的场景和物体映射。

输出：

- 一份结构化 JSON 计划。

典型计划不是连续的关节控制，而是四个高层步骤：

```text
move(input_5)
pick_up(input_5, line_5_container_h01_near)
move(output_4)
place_down(output_4)
```

因此，LLM 是“任务规划者”，不是直接输出每个电机动作的控制器。

### 第二步：Agent 按顺序调用技能

`RobotAgent` 读取 LLM 给出的计划，查找对应技能并逐步执行。  
技能在 `src/robot_agent/skills/library.py` 中注册。

每个技能都接收统一的 `ExecutionContext`，返回统一的 `SkillResult`。

可以先把它理解成：

```text
LLM 给出步骤单
    ↓
RobotAgent 像调度员一样逐项执行
    ↓
每个技能报告成功/失败和执行信息
```

### 第三步：四个核心技能各自负责不同层次

#### `move`

输入：

- 目标站点名，例如 `input_5`；
- 当前机器人底盘位置；
- 语义地图中的站点接近点；
- 占据栅格。

处理：

1. 把站点名解析成世界坐标中的接近点；
2. 把世界坐标转成栅格坐标；
3. 用 A* 找路；
4. 把路径转回世界坐标并简化；
5. 交给 backend 驱动底盘沿路径移动。

输出：

- 是否到达；
- 起点、终点、最终位置；
- 路径点数量。

#### `pick_up`

输入：

- 取货站名；
- 精确物体名；
- 当前或训练时的抓取底盘位姿；
- BC checkpoint；
- 机器人视角图像和低维机器人状态。

处理：

1. 加载 robomimic BC 抓取策略；
2. 在抓取环境中运行策略若干步；
3. 检查抓取结果；
4. 再执行抬升并验证物体是否稳定；
5. 成功后把“已抓住的物体”同步回导航环境。

输出：

- 抓取并抬升是否成功；
- 当前持有的物体名；
- 抓取过程轨迹事件。

当前代码中，BC checkpoint 直接用于 `pick_up` 的抓取动作。

#### 第二次 `move`

和第一次使用同一套导航逻辑，但机器人现在携带物体。

#### `place_down`

输入：

- 放货站名；
- 当前持有的物体；
- 放置和转向参数。

当前代码的处理：

1. 面向目标输出站；
2. 计算桌面和物体的安全释放高度；
3. 降低物体；
4. 松开夹爪；
5. 等待物体在重力作用下落到台面。

当前实现是参数化的物理放置流程，没有调用抓取用的 BC checkpoint。

### 第四步：记录和评分

`app.py` 在执行前开始记录轨迹，执行后：

- 保存 trajectory JSON；
- 根据物体是否被成功抓取、是否离开源站、是否到达目标站附近、是否发生碰撞来评分；
- 可从轨迹生成俯视或机器人视角的回放。

这说明“训练模型”不是比赛的最终输出。模型只是在线执行链路中的一个零件；最终输出是完整任务轨迹和得分。

## 3. 三种容易混在一起的“坐标”

现在先只记住分类，不在本课追究每个数字如何测得：

1. **站点中心坐标**  
   描述桌子或站点在哪里，主要来自场景地图 JSON。

2. **站点接近点（approach）**  
   机器人导航应该停到哪里，通常位于桌子旁而不是桌子中心；A* 的目标主要使用它。

3. **抓取训练位姿（grasp pose）**  
   BC 模型训练或采集时机器人底盘的精确位置和朝向。它对图像视角和机械臂相对位置非常敏感，目前保存在 `knowledge/task_config.json` 的 `grasp_poses` 中。

因此，“站点中心”“导航终点”“模型抓取姿态”不是同一个坐标。

## 4. 两个已经通过代码确认的边界

### 边界一：原始 scene JSON 主要是导航地图数据，不是抓取示范脚本的直接动作表

`maps/...scene_regenerated.json` 包含场景边界、站点、障碍物、机器人起点等信息。  
`get_map.py` 把它转换成：

- `*_semantic_map.json`
- `*_occupancy_grid.npy`
- `*_map.png`

随后 `move` 使用语义地图和占据栅格规划路径。

L1 的 `collect.py` 本身没有直接读取这个 JSON 来生成机械臂动作。它创建的是对应的 FactorySorting 仿真环境，并根据仿真中物体的抓取标记位置，程序化地产生一条双臂抓取示范。

### 边界二：当前 SOP 代码只完成了“读取”，还没有形成完整的“自动生成 Markdown”闭环

`skills/read_document.py` 当前能够：

- 读取 `.docx` 段落；
- 提取图片；
- 调用 VLM 描述图片；
- 把文字和图片描述作为 `SkillResult.payload` 返回。

但当前仓库中没有发现把这些结果自动整理并写成 `sop1.md` 至 `sop5.md` 的完整实现。  
这意味着 SOP 自动生成链路正是一个需要参赛者补齐或优化的部分，不能把现有参考 Markdown 当成已经由当前代码自动生成。

## 5. 本课应该形成的心智模型

请先用一句话记住：

> LLM 负责选择和排列技能，地图负责导航，BC 模型负责抓取动作，参数化物理流程负责当前的放置，MuJoCo 负责模拟和判断真实执行结果。

更短的版本：

```text
LLM 决定“做哪几步”
技能决定“每一步调用什么程序”
地图决定“底盘怎么走”
BC 模型决定“抓取时手怎么动”
仿真器决定“物理上到底成没成功”
```

## 6. 接下来的推荐学习顺序

暂时不要马上改路径算法或训练模型。建议按以下顺序建立可验证的理解：

1. 彻底看懂一条 L1 在线执行链：任务 → 四步计划 → 四个技能 → 轨迹 → 得分。
2. 深入 `collect.py` 的一次 rollout：reset、接近、闭爪、成功判断、筛选。
3. 打开一份 HDF5，逐项查看 `obs`、`actions`、`states` 的形状和含义。
4. 再看 robomimic 如何把 HDF5 训练成 `.pth`。
5. 对照在线 `pick_up`，确认训练输入和推理输入是否一致。
6. 最后再分别优化导航、抓取数据、训练参数和放置逻辑。

## 7. 本课停点

本轮只要求能够区分：

- 在线执行与离线训练；
- 高层 LLM 计划与低层机器人动作；
- 导航地图与抓取数据；
- 站点中心、导航接近点、抓取训练位姿。

在进入下一课前，可以围绕这四组区别提问。

---

# 待深入问题清单

- [ ] `collect.py` 的一条成功 rollout 具体包含哪些阶段？
- [ ] HDF5 的 `states`、`actions`、`obs` 分别是什么？
- [ ] 为什么采集脚本只保存成功 episode？失败数据是否完全没用？
- [ ] `scene.py` 和 `collect.py` 的区别是什么？
- [ ] BC 模型的训练命令、配置文件、输入 shape 和输出 action 是什么？
- [ ] 训练抓取模型时，机器人底盘位置和 yaw 为什么必须匹配？
- [ ] `pick_up` 成功是如何判断的？抓住与抬起为什么是两次判断？
- [ ] 当前 `place_down` 为什么没有使用 BC 模型？是否值得训练单独的放置模型？
- [ ] scene JSON、semantic map、occupancy grid 三者如何转换？
- [ ] SOP 图片中的坐标如何可靠提取、校验并写入 Markdown？
- [ ] 现有 `sop1.md`、`task_config.json` 和地图坐标之间有哪些来源差异？

# 变更记录

- 2026-07-28：创建日志；完成第 1 课“系统分层与端到端数据流”。
- 2026-07-28：完成第 2 课“一次抓取示范怎样被采集”。

---

# 第 2 课：一次抓取示范怎样被采集

本课只看：

`load_factory_sorting_1_3fo3erfhisem_collect.py`

目标不是马上学会修改它，而是回答一个基础问题：

> 一条 HDF5 demonstration 在成为训练数据之前，机器人到底做了什么？

## 1. 先理解 demonstration 和 rollout

- **rollout**：从环境重置开始，到本次尝试结束的一整次机器人运行。
- **episode**：保存到数据集中的一次完整时序记录。在这份脚本里，可以先把它和一次 rollout 看成同一层概念。
- **demonstration**：作为“正确示范”交给模型模仿的 episode。

脚本默认尝试 20 次 rollout，但不是 20 次都会写进最终 HDF5：

```text
尝试 1 → 成功 → 保存
尝试 2 → 失败 → 丢弃
尝试 3 → 成功 → 保存
……
```

因此：

```text
num_rollouts = 尝试次数
num_successful_demos = 最终保留的成功示范数
```

## 2. 采集开始前已经固定了什么

这份 L1 采集脚本的默认条件包括：

- 场景：`FactorySorting1_3FO3ERFHISEM`
- 机器人：Tiago
- 目标物体：`line_5_container_h01_near`
- 底盘位置：约 `(8.0, 4.6, 0.0)`
- 底盘 yaw：约 `3.139`
- 相机：`robot0_robotview`
- 图像尺寸：`128 × 128`
- rollout 数：20

这意味着它不是从工厂入口开始导航，也不是随机寻找箱子。

它假定：

> 导航已经结束，机器人已经站在适合抓取的固定位置和朝向。

所以，这份数据主要训练“近距离机械臂抓取”，没有训练从机器人起点走到取货站的导航。

## 3. 一次 rollout 的完整顺序

### 阶段 0：重置环境

调用：

```text
env.reset()
```

结果：

- 机器人回到指定底盘位姿；
- 物体回到场景初始状态；
- 创建一个新的观察缓冲区；
- 找到目标物体左右两侧的抓取标记点。

目标物体上预先定义了：

```text
line_5_container_h01_near_right_grasp_site
line_5_container_h01_near_left_grasp_site
```

这些 `site` 是 MuJoCo 中的辅助标记。它们告诉采集脚本：

- 右臂的夹爪应该靠近哪里；
- 左臂的夹爪应该靠近哪里。

它们不是神经网络预测出来的。

### 阶段 1：双臂先向上移动到安全高度

默认约 60 步。

目的：

- 避免手臂直接横穿桌面或物体；
- 先把两只手提升到安全高度。

此时夹爪保持张开。

### 阶段 2：在安全高度进行 XY 平移

默认约 120 步。

两只手分别移动到各自抓取点的正上方。  
此阶段主要改变水平位置，仍然保持安全高度和张开状态。

如果在本不该接触物体的阶段提前碰到箱子，这条 rollout 会被判失败。

### 阶段 3：垂直下降到抓取点附近

默认约 80 步。

两只手从上方向下移动到物体左右两侧。  
目标位置会略低于物体上定义的 grasp site，偏移量默认为 `0.035 m`。

### 阶段 4：小范围稳定和误差修正

前面的插值移动不保证机械臂刚好到达目标，因此脚本继续进行反馈修正：

```text
当前位置
  → 与目标位置求差
  → 转换到机械臂控制器坐标系
  → 生成下一步 action
  → 再测距离
```

它检查两类位置：

1. 机械臂末端执行器 EEF 是否接近目标；
2. 夹爪两片指垫的中心是否接近目标。

默认容差大致为：

- EEF：`0.025 m`
- 夹爪末端中心：`0.03 m`

超过容差且在稳定步数内无法修正，就判失败，不继续闭爪。

### 阶段 5：闭合双侧夹爪

默认约 40 步。

采集脚本把两侧 gripper action 设置为闭合，手臂位置基本保持。

随后分别检查：

- 左夹爪是否对目标物形成 grasp；
- 右夹爪是否对目标物形成 grasp；
- 指垫和物体之间是否存在接触。

代码要求：

```text
左夹爪 grasp = True
并且
右夹爪 grasp = True
```

只抓住一侧不算成功。

### 阶段 6：成功后继续保持

默认约 10 步。

两只夹爪继续闭合并保持物体。然后本次 rollout 返回：

```text
success: both grippers grasped and held the object
```

注意：

> 这份采集脚本在这里没有执行“向上抬起 0.15 m”的验证。

在线比赛的 `pick_up` 会在 BC 策略结束后另外调用 `lift_grasped_object()`，把“策略判断抓住”和“实际抬起并保持”组合成最终成功条件。  
所以采集成功标准和在线 `pick_up` 最终成功标准并不完全相同。

## 4. 每一个动作步骤记录了什么

脚本并不是动作做完后才拍一张结果图，而是在每次 `env.step(action)` 前记录当前观察。

顺序是：

```text
记录当前 observation_t
  → 执行 action_t
  → MuJoCo 进入下一状态
```

因此模型训练时学习的是：

```text
看到 observation_t 时，应该输出 action_t
```

这就是行为克隆最核心的一对数据：

```text
输入：当前看到了什么、机器人现在是什么状态
标签：示范老师在这一刻采取了什么动作
```

## 5. observation 中实际保存的七项

每一步保存：

```text
robot0_left_eef_pos
robot0_left_eef_quat
robot0_left_gripper_qpos
robot0_right_eef_pos
robot0_right_eef_quat
robot0_right_gripper_qpos
robot0_robotview_image
```

可以先分成两组：

### 低维状态

- 左右手末端位置；
- 左右手末端姿态；
- 左右夹爪开合状态。

### 图像状态

- 机器人胸前相机看到的 `128 × 128` RGB 图像。

模型不是只看图片，也不是只看坐标；它得到的是图像与机器人自身状态的组合。

## 6. action 是谁产生的

这里没有人工拿手柄遥操作，也没有训练好的 BC 模型。

action 由采集脚本根据几何误差程序化生成：

```text
目标抓取点 - 当前机械臂位置
  → 坐标系转换
  → 归一化并限制最大动作
  → 组装左右臂、左右夹爪、躯干、头部、底盘 action
```

因此采集脚本本身就是“专家策略”或“示范老师”。

BC 模型之后要做的，是学习模仿这个脚本：

```text
专家脚本：根据精确 site 和当前位置算 action
                         ↓ 生成训练对
BC 模型：根据 observation 预测相似 action
```

两者的一个重要区别是：

- 专家脚本在采集时可以使用仿真内部的精确抓取点；
- 训练后的模型在比赛推理时主要依靠它训练时配置的 observation。

## 7. 原始记录怎样变成最终 HDF5

采集过程中，`DataCollectionWrapper` 先把每个 episode 的内容写到临时目录：

```text
ep_xxx/
├── model.xml
├── ep_meta.json
└── state_xxx.npz
```

其中：

- `model.xml`：当次 MuJoCo 模型；
- `states`：MuJoCo 完整仿真状态；
- `action_infos`：执行过的 action；
- `successful`：该段记录是否被标记为成功；
- 单独的内存 `obs_buffer`：七项 observation。

全部 rollout 完成后，`gather_successful_demonstrations_as_hdf5()` 才执行筛选和合并。

筛选逻辑：

```text
没有 state → 跳过
successful = False → 跳过
successful = True → 写入 demo_N
```

最终结构可以先理解为：

```text
data/
├── demo_1/
│   ├── states
│   ├── actions
│   └── obs/
│       ├── robot0_left_eef_pos
│       ├── ...
│       └── robot0_robotview_image
├── demo_2/
│   └── ...
└── attributes
    ├── env_args
    ├── policy_info
    └── num_successful_demos
```

图像数据使用 gzip 压缩，其他低维数据直接保存。

写入前还会强制检查：

```text
每一种 observation 的时间步数 == action 的时间步数
```

如果对不上，脚本报错，不会悄悄生成一份错位数据集。

## 8. 用一条抽象示范来理解

假设某次成功 rollout 有 350 个动作步骤：

```text
t=0：看到双臂初始位置和机器人视角 → action_0：双臂向上
t=1：看到移动一点后的状态         → action_1：继续向上
……
t=180：双臂位于箱子上方           → action_180：开始下降
……
t=310：夹爪来到箱子两侧           → action_310：闭合夹爪
……
t=349：双夹爪已闭合               → action_349：继续保持
```

最终写入：

```text
350 个 observations
350 个 actions
对应的 MuJoCo states
```

训练时，模型会大量看到“某种 observation 对应某种 action”的例子，从而拟合专家行为。

## 9. 本课最重要的三个结论

1. `collect.py` 是产生示范的专家脚本，不是训练脚本，也不是调用现成模型采集。
2. 一条 demonstration 包含整个双臂接近、下降、闭爪和保持过程，不只是成功瞬间的一张图片。
3. 最终 HDF5 只保留成功 rollout；当前采集成功表示双夹爪形成 grasp 并保持，不包含在线执行中的抬升验证。

## 10. 理解检查题

1. 为什么采集脚本要从固定的 `(8.0, 4.6, yaw≈3.139)` 附近开始，而不是从机器人初始位置开始？
2. 在一条训练数据中，`observation_t` 和 `action_t` 分别表示什么？
3. 如果左夹爪抓住了、右夹爪没有抓住，这条 rollout 会不会进入最终 HDF5？为什么？
4. `collect.py` 和训练后的 BC 模型，谁是老师，谁是学生？
5. 这份采集脚本判断“成功”后，有没有真正把箱子向上抬起验证？

## 11. 学习者回答与掌握情况

学习者回答：

1. `(8.0, 4.6)` 是拿箱子时正确的站位，`3.139` 是机器人朝向。
2. observation 是机器人当前视角与状态；action 是机器人在当前信息下执行的事情。
3. 只有一侧夹爪抓住属于失败，不会写入最终 HDF5。
4. `collect.py` 是老师，BC 是通过成功示范训练出来、模仿老师的模型。

判断：

- 前四项理解正确。
- 后续需要进一步区分“抓取位姿”和完整六自由度位姿的概念；当前学习阶段把它理解为底盘位置加朝向即可。
- 第 5 项已在本课说明：采集脚本没有执行抬升验证，在线 `pick_up` 才追加抬升验证。

---

# 补充课：read_document 技能与两种“场景”

## 1. `read_document.py` 是不是当前的一个 skill？

是。

它是一个辅助技能，类名为：

```text
ReadDocumentSkill
```

技能名为：

```text
read_document
```

它在 `skills/library.py` 的 `wired_skills()` 中注册，因此和 `move`、`pick_up`、`place_down` 一样，属于 Agent 的技能注册表。

当前有两种调用路径：

1. Agent/LLM 计划中以技能名 `read_document` 调用；
2. `app.py` 侧边栏的 `Test ReadDocumentSkill` 按钮直接构造并调用它。

但必须区分：

```text
“它是一个 skill”
不等于
“它已经完成 Word → sop1.md 的全部工作”
```

它当前的输出是一个 `SkillResult`，payload 中包含：

- Word 段落文字；
- 提取出的图片数量；
- VLM 对图片的描述。

它当前没有负责把这些内容进一步校验、整理并写成最终 SOP Markdown。

## 2. 为什么说 scene JSON 和 collect 使用的 MuJoCo 场景不是同一个东西？

可以用“二维建筑平面图”和“真实建筑”来类比。

### scene JSON：给导航看的二维平面图

文件：

```text
maps/factory_sorting_1_3fo3erfhisem_scene_regenerated.json
```

里面主要是：

- 地图边界；
- 站点中心；
- 站点长宽；
- 导航接近点；
- 障碍物占用范围；
- 机器人导航起点。

它的处理链路是：

```text
scene JSON
  → get_map.py
  → semantic_map.json
  → occupancy_grid.npy
  → move 使用 A* 找路
```

这一套主要回答：

> 底盘在二维地面上从哪里走到哪里，哪里不能撞。

### MuJoCo 场景：机器人真正执行动作的三维物理世界

`collect.py` 创建：

```text
FactorySorting1_3FO3ERFHISEM
```

这个仿真环境主要由 FactorySorting 的 Python 环境类和 MuJoCo 场景 XML 构建。里面不仅有二维位置，还有：

- 物体三维模型；
- 质量、摩擦和碰撞几何体；
- 机器人关节；
- 左右机械臂控制器；
- 相机；
- 夹爪接触；
- 物体左右抓取 site；
- 每一时刻完整的物理状态。

这一套主要回答：

> 机械臂怎样移动、夹爪有没有接触箱子、物理上是否真正抓住。

它的采集链路是：

```text
FactorySorting Python 环境 + MuJoCo scene XML
  → 创建三维仿真
  → collect.py 查询物体的 left/right grasp site
  → 根据当前手臂位置和 site 位置计算 action
  → MuJoCo 执行动作并判断接触
  → 成功示范写入 HDF5
```

## 3. 两条链路的关系

```text
二维导航链：
scene JSON → 栅格地图 → move

三维抓取链：
MuJoCo XML + 环境 Python → 物理仿真 → collect → HDF5
```

它们描述的是同一座工厂，因此使用同一个世界坐标系，位置应该互相对应；但它们是两种不同用途的数据表示。

关键区别：

| 对比项 | scene JSON | MuJoCo 场景 |
| --- | --- | --- |
| 类比 | 建筑平面图 | 真实建筑 |
| 维度 | 主要是地面 XY | 完整三维物理 |
| 主要用户 | `move` / A* | 机械臂、夹爪、相机、碰撞 |
| 能否判断夹爪接触 | 不能 | 能 |
| 是否包含抓取 site | 导航 JSON 不负责 | MuJoCo 物体中包含 |
| `collect.py` 是否直接打开它 | 否 | 是，通过创建仿真环境 |

因此原句的准确含义是：

> `collect.py` 没有打开导航用的 scene JSON，再从 JSON 中读取一串抓取动作。它创建了同名的三维 MuJoCo 工厂环境，在仿真内部读取箱子左右抓取点和机械臂当前位置，现场计算每一步动作。

## 4. 下一轮理解检查

1. scene JSON 更像“平面图”还是“真实物理工厂”？
2. A* 导航主要使用哪条链路？
3. `collect.py` 从哪里获得箱子左右两侧的精确抓取点？
4. 为什么只靠 scene JSON 不能判断夹爪是否真正抓住箱子？
5. `read_document` 当前输出了文档内容，是否等于已经生成了最终 `sop1.md`？

## 5. 学习者回答与新问题

已掌握：

- scene JSON 是工厂平面图。
- `collect.py` 使用 MuJoCo 场景。
- scene JSON 只是场景描述，本身不能完成抓取物理判断。
- `read_document` 后面还需要让 LLM 整理内容并形成格式化输出。

新问题：

1. MuJoCo 场景是否根据 scene JSON 创建？
2. 抓取点和物理状态具体从哪里来？
3. 最终是谁判断夹爪是否抓住：MuJoCo 还是 `collect.py`？

---

# 补充课 2：MuJoCo、抓取点、物理状态和成功判断

## 1. MuJoCo 场景是不是根据 scene JSON 创建的？

在当前项目中：**不是**。

实际的来源方向更接近：

```text
MuJoCo scene XML
+ FactorySorting Python 环境
+ 三维 mesh / summary 数据
            ↓
创建三维 MuJoCo 物理场景
            ↓
整理 / 再生成二维 scene JSON
            ↓
为导航生成 semantic map 和 occupancy grid
```

L1 scene JSON 自己的 `source_files` 字段列出了它的来源：

- `factory_sorting_1_3fo3erfhisem.py`
- `load_factory_sorting_1_3fo3erfhisem_scene.py`
- `scene_robosuite.xml`
- `summary.json`
- 三维 mesh 文件

这说明当前仓库中的 scene JSON 是根据现有场景代码、XML 和三维资源整理出的二维导航表示，而不是反过来用 JSON 创建 MuJoCo 三维场景。

可以把关系理解成：

```text
三维 MuJoCo 工厂是“原建筑”
scene JSON 是从原建筑整理出的“导航平面图”
```

`get_map.py` 再把这张导航平面图转换成 A* 方便使用的栅格。

## 2. 抓取点从哪里来？

抓取点由 FactorySorting 环境代码添加到目标箱子的 MuJoCo 模型中。

对于：

```text
line_5_container_h01_near
```

环境代码创建两个球形辅助 site：

```text
line_5_container_h01_near_right_grasp_site
line_5_container_h01_near_left_grasp_site
```

这两个点相对于箱子本体的位置，在环境代码中明确设置：

```text
right site: [face_x, +0.120, top_z]
left site:  [face_x, -0.120, top_z]
```

其中：

- `face_x = 0.29`
- `top_z` 来自箱子模型顶部高度
- `±0.120` 把左右抓取点分别放在箱子两侧

这些是箱子局部坐标。MuJoCo 把箱子放进世界后，会计算它们在世界坐标系中的实时位置：

```text
env.sim.data.site_xpos[site_id]
```

`collect.py` 的 `site_pos()` 和 `get_target_positions()` 就是从这里读取抓取点。

因此：

```text
环境 Python 代码定义抓取点
→ MuJoCo 模型保存抓取点
→ MuJoCo 计算抓取点当前世界坐标
→ collect.py 读取坐标并控制机械臂靠近
```

## 3. 物理状态从哪里来？

物理状态由 MuJoCo 仿真器在每次 `env.step(action)` 后计算并保存在 `sim.data` 中，包括：

- 机器人关节位置和速度；
- 左右机械臂末端位置；
- 夹爪开合状态；
- 箱子的位置、姿态和速度；
- site 的世界坐标；
- 当前所有碰撞接触对；
- 相机渲染出的图像。

可以理解为：

```text
collect.py 提交 action
        ↓
MuJoCo 根据质量、关节、碰撞、摩擦等计算下一时刻
        ↓
MuJoCo 更新 sim.data
        ↓
collect.py 再读取新的状态
```

`collect.py` 不负责求解完整物理过程，它负责提出动作、读取结果和组织采集流程。

## 4. 到底是谁判断“抓住了”？

最准确的回答是：

> MuJoCo 提供真实接触数据，robosuite 提供通用抓取检查函数，`collect.py` 调用检查函数并制定本任务的成功规则。

分三层：

### 第一层：MuJoCo

MuJoCo 计算夹爪和箱子之间发生了哪些碰撞接触，并将接触对放在：

```text
env.sim.data.contact
```

### 第二层：robosuite

robosuite 的：

```text
env._check_grasp(gripper, object_geoms)
```

检查一只夹爪的两组指垫是否都接触目标物体。

一只夹爪的基本规则是：

```text
左指垫接触箱子
并且
右指垫接触箱子
→ 这一只夹爪 grasp=True
```

### 第三层：collect.py

`collect.py` 对左右两条机械臂分别调用 `_check_grasp()`：

```text
右臂夹爪 grasp=True
并且
左臂夹爪 grasp=True
→ 本次双臂抓取通过
```

所以不能只说“collect 判断”，也不能只说“MuJoCo 判断”：

- MuJoCo 是传感器和物理事实的提供者；
- robosuite 把接触事实解释成单夹爪抓取；
- `collect.py` 规定双臂都通过才算本次采集成功。

## 5. 关于 SOP 格式化理解的修正

“还缺少给 LLM 读并写出格式化过程”方向正确，但要补上两个环节：

```text
read_document 输出
→ LLM/VLM 提取结构化字段
→ 与地图、题目和勘误信息交叉校验
→ 按知识库模板生成 Markdown
→ 写入 knowledge/
→ 刷新知识库索引
→ 规划器把 Markdown 注入 prompt
```

不能只让 LLM 自由总结后直接写文件，因为站点编号、坐标和物体名一旦总结错误，会让后续整个执行计划出错。

## 6. 理解检查

1. 当前项目的因果方向更接近“scene JSON 生成 MuJoCo”，还是“从 MuJoCo 场景资源整理出 scene JSON”？
2. 抓取 site 是神经网络预测的，还是环境代码预先定义的？
3. MuJoCo、robosuite 的 `_check_grasp()`、`collect.py` 三者分别负责什么？
4. 如果一只夹爪只有一个指垫碰到箱子，`_check_grasp()` 会返回 True 吗？

## 7. 重点澄清：collect 到底是产生 action，还是获取 action？

学习者已正确理解：

> 抓取点已经写在三维箱子模型中。MuJoCo 把箱子放到世界里以后，`collect.py` 能读取抓取点当前的世界坐标。

需要澄清的歧义：

在这份采集脚本中，`collect.py` **自己计算并产生专家 action**，然后把 action 交给 MuJoCo 执行；记录工具再把同一个 action 保存下来。

一次最小循环是：

```text
1. collect 读取当前手的位置
2. collect 读取目标抓取点的位置
3. collect 计算两者差多少
4. collect 根据差值生成 action
5. collect 把 action 交给 MuJoCo
6. MuJoCo 计算机器人实际移动后的新状态
7. collect 读取新状态
8. 回到第 1 步
```

例如只看 X 方向：

```text
目标抓取点 x = 1.00
当前手位置 x = 0.80
还差             = 0.20
```

`collect.py` 据此产生一个“向正 X 移动”的 action。MuJoCo 执行后，手可能移动到 `x = 0.86`。下一步：

```text
目标抓取点 x = 1.00
新手的位置 x = 0.86
还差             = 0.14
```

`collect.py` 再产生下一个 action。

因此，采集阶段不是：

```text
模型 → action → collect
```

而是：

```text
collect 的专家规则 → action → MuJoCo
                         ↓
                    保存到 HDF5
```

三个阶段中的 action 来源不同：

| 阶段 | 谁产生 action | action 用途 |
| --- | --- | --- |
| 数据采集 | `collect.py` 的专家规则 | 控制 MuJoCo，同时保存成标准答案 |
| BC 训练 | HDF5 提供历史 action | 作为模型要模仿的标签 |
| 在线抓取 | 训练后的 BC 模型 | 控制真实的仿真抓取动作 |

一句话记忆：

> 采集时 collect 写答案；训练时 BC 学答案；比赛时 BC 自己答题。

当前暂停点：

- 暂时不继续展开 `_check_grasp()` 的内部三层结构。
- 先确保“抓取点、当前状态、action、物理结果”这四个概念清楚。

## 8. 理解检查纠正

学习者回答：

1. `attention`（推测想表达 `action`）
2. BC

纠正：

```text
目标位置 + 当前夹爪位置
          ↓
collect.py 负责计算
          ↓
产生 action
          ↓
MuJoCo 负责执行 action 并计算物理结果
```

所以：

- `collect.py` 是计算者；
- `action` 是计算出来的动作命令；
- MuJoCo 是动作执行和物理计算者；
- BC 在采集阶段还没有参与，它是之后用数据训练出的学生。

最短记忆：

```text
采集：collect → action → MuJoCo
训练：HDF5 → BC
比赛：observation → BC → action → MuJoCo
```

学习者第二次回答：

```text
采集时：collect
比赛时：BC
```

判断：回答正确，本知识点通过。

---

# 第 3 课：拿什么判断夹爪是否抓住

本课只区分三个角色：

```text
MuJoCo：产生接触事实
robosuite：把接触事实检查为单夹爪 grasp
collect：规定双臂都 grasp 才算采集成功
```

## 1. MuJoCo 产生接触事实

MuJoCo 每执行一次 action，就计算场景中哪些碰撞表面正在接触。

例如：

```text
右臂夹爪左指垫 ↔ 箱子
右臂夹爪右指垫 ↔ 箱子
左臂夹爪左指垫 ↔ 箱子
左臂夹爪右指垫 ↔ 箱子
```

这些接触对保存在 MuJoCo 的实时物理状态中。

抓取 site 不负责判断接触。它只是一个目标位置标记。

## 2. robosuite 检查一只夹爪

robosuite 的 `_check_grasp()` 检查：

```text
一只夹爪的左指垫接触目标物体
并且
同一只夹爪的右指垫也接触目标物体
```

两边都有接触，才返回：

```text
grasp = True
```

只有一边碰到，返回 False。

## 3. collect 检查两条机械臂

这台 Tiago 使用左右两条机械臂共同侧夹箱子。

因此 `collect.py` 分别检查：

```text
右机械臂夹爪 grasp
左机械臂夹爪 grasp
```

最终规则：

```text
右臂 grasp=True
并且
左臂 grasp=True
→ rollout 成功
```

否则：

```text
→ rollout 失败
→ 不进入最终 HDF5
```

## 4. 一句话区分抓取点和抓取成功

```text
grasp site：告诉夹爪“应该去哪里”
contact/grasp check：判断夹爪“实际上有没有夹住”
```

## 5. 理解检查

1. 抓取 site 是导航目标，还是抓取成功证明？
2. 一只夹爪只有一个指垫接触箱子，是否算 grasp？
3. 左臂抓住但右臂没抓住，这次 rollout 是否成功？

## 6. 学习者回答

1. grasp site 是目标位置。
2. 一只夹爪只有一个指垫接触，不算 grasp。
3. 左臂抓住但右臂没有抓住，不算成功，因为 collect 只收集双臂都抓取成功的信息。

判断：三项全部正确，第 3 课通过。

---

# 第 4 课：HDF5 中的 obs、actions、states

本课只区分三个数据类别，不学习具体数组形状。

一条成功 demonstration 中，每个时间步大致都有：

```text
obs_t
action_t
state_t
```

## 1. obs：给策略看的信息

`obs` 是 observation 的缩写。

这份采集脚本保存：

- 左右手末端位置；
- 左右手末端姿态；
- 左右夹爪开合状态；
- 机器人胸前相机图像。

可以理解成：

```text
obs = 学生答题时能够看到的题目
```

训练配置会决定七项 observation 中哪些实际输入 BC。在线执行时，checkpoint 也会声明自己需要哪些 observation key；执行代码只把模型需要的 key 交给模型。

## 2. actions：专家标准答案

`actions` 是 collect 专家规则在每一步计算出的动作命令。

例如：

- 左右臂向什么方向移动；
- 移动幅度多大；
- 夹爪张开还是闭合；
- 其他受控部件保持什么状态。

可以理解成：

```text
action = 老师针对当前题目给出的标准答案
```

BC 训练的核心目标：

```text
BC(obs_t) ≈ action_t
```

即模型看到相同或相似 observation 时，预测的 action 要尽量接近 collect 的 action。

## 3. states：MuJoCo 完整存档

`states` 是 MuJoCo 仿真器更完整的内部状态快照。

它比给 BC 看的 observation 更全面，可能包含：

- 所有相关关节状态；
- 物体完整位置和姿态；
- 仿真器内部状态；
- 恢复场景所需的信息。

可以理解成：

```text
state = 游戏的完整存档
```

它主要适合：

- 恢复某个仿真时刻；
- 重放 demonstration；
- 从完整状态重新生成 observation；
- 检查数据是否能在同一环境中复现。

普通 BC 学习的直接关系主要是：

```text
obs → action
```

不是：

```text
state → action
```

但具体训练配置仍是最终依据，不能仅凭 HDF5 中“存在某个字段”就断定模型一定使用它。

## 4. 三者放在同一时刻理解

假设时间步 `t=100`：

```text
obs_100:
  相机看到箱子在双臂中间
  左右手位置
  左右夹爪当前张开

action_100:
  双臂继续靠近
  夹爪保持张开

state_100:
  此刻整个 MuJoCo 仿真的完整存档
```

下一时间步：

```text
执行 action_100
→ MuJoCo 计算
→ 得到 state_101
→ 从新状态得到 obs_101
→ collect 再计算 action_101
```

## 5. 最短记忆

```text
obs     = 模型看到的题目
actions = collect 给出的标准答案
states  = MuJoCo 的完整存档
```

## 6. 理解检查

1. BC 主要学习的是 `obs → action`，还是 `state → action`？
2. 哪一个更像游戏完整存档：obs、actions 还是 states？
3. HDF5 里保存了某个 observation，是否必然代表训练配置一定把它输入模型？

## 7. 学习者回答与纠正

学习者回答：

1. `obs`，理解为 `obs → action`：正确。
2. `states`：正确。
3. HDF5 保存相机图像，就必然输入 BC：不正确。

需要区分：

```text
HDF5 中保存了某项数据
≠
训练配置选择了该项数据
```

例如数据仓库中有：

```text
obs/
├── robot0_robotview_image
├── robot0_left_eef_pos
├── robot0_right_eef_pos
└── ...
```

训练配置 A 可以选择：

```text
图像 + 左右手状态
```

训练配置 B 也可以选择：

```text
只使用左右手状态，不使用图像
```

因此正确关系是：

```text
collect 决定数据集能够提供什么
训练配置决定 BC 实际使用什么
checkpoint 保存训练后模型实际要求什么
```

在线推理时，执行代码会读取 checkpoint 所要求的 observation keys，并把相应 observation 交给策略。如果所需 key 缺失，会直接报错。

本知识点暂不继续推进，等待学习者确认“数据存在”和“训练选用”的区别。

学习者再次回答：

1. HDF5 有图像，但配置只选择低维状态时，模型不一定使用图像。
2. 训练配置决定 BC 使用哪些 observation。

判断：回答正确，第 4 课通过。

---

# 第 5 课：当前 checkpoint 实际使用哪些 observation

已直接检查当前仓库中的：

```text
robosuite/robosuite/model_epoch_150.pth
```

checkpoint 内保存的配置表明，这个模型确实同时使用低维状态和机器人视角图像。

## 1. 六项 low_dim 输入

```text
robot0_left_eef_pos
robot0_left_eef_quat
robot0_left_gripper_qpos
robot0_right_eef_pos
robot0_right_eef_quat
robot0_right_gripper_qpos
```

含义：

- 左手末端位置；
- 左手末端姿态；
- 左夹爪状态；
- 右手末端位置；
- 右手末端姿态；
- 右夹爪状态。

## 2. 一项 rgb 输入

```text
robot0_robotview_image
```

即机器人胸前相机图像。

因此当前模型的输入可以概括为：

```text
左右手自身状态
+
机器人第一视角图像
```

结论：

> “HDF5 中有图像，不代表所有训练一定使用图像”是一般规则；但这个具体 checkpoint 的训练配置明确选择了图像，所以当前模型确实看图像。

## 3. 图像怎样进入模型

checkpoint 配置中，图像编码器使用：

```text
ResNet18Conv
+ SpatialSoftmax
```

当前阶段只需要理解：

```text
128 × 128 RGB 图像
→ 图像编码器提取视觉特征
→ 与六项低维机器人状态结合
→ BC 网络预测 action
```

暂时不展开卷积网络和 SpatialSoftmax 的数学细节。

## 4. 在线执行

在线评估代码会：

```text
读取 checkpoint 需要的 observation keys
→ 从当前 MuJoCo 环境取得对应 observation
→ 交给 policy
→ policy 输出 action
→ MuJoCo 执行
```

如果 checkpoint 要求某个 observation，但当前环境没有提供，会报缺失 key 错误。

## 5. 一个需要后续核对的文件命名异常

外部文件名是：

```text
model_epoch_150.pth
```

但 checkpoint 压缩包内部目录名显示为 `model_epoch_500`，配置的总训练轮数也是 500。仅凭这些还不能断定它实际保存于第几轮；后续讲 checkpoint 元数据时再核对，不在本课展开。

## 6. 理解检查

1. 当前具体的 checkpoint 是否使用机器人相机图像？
2. 除图像外，模型还接收哪一大类信息？
3. 一般情况下“HDF5 有图像”和“当前这个 checkpoint 使用图像”是否是同一个判断依据？

## 7. 学习者总结与精确修正

学习者当前理解：

```text
训练：
当前 obs → BC → 预测 action
让预测 action 尽量接近 collect 的标准 action

使用：
当前 obs → 训练好的 BC → action
```

这个主干理解正确。

三道理解题也回答正确：

1. 当前 checkpoint 使用机器人相机图像。
2. 除图像外，还使用左右手位姿、姿态和夹爪信息。
3. checkpoint 中保存的配置能够证明当前模型使用图像。

需要精确修正：

> `collect.py` 不是严格地只根据保存给 BC 的七项 obs 计算 action。

采集专家还使用了 MuJoCo 内部的精确信息，例如：

- 物体左右 grasp site 的世界坐标；
- 机械臂当前精确位置；
- 控制器坐标转换信息；
- 接触和抓取检查结果。

其中一部分信息不一定包含在训练模型最终看到的七项 observation 中。

更准确的采集关系是：

```text
MuJoCo 内部精确信息
        ↓
collect 专家规则计算标准 action
        ↓
同时记录：
选定的 obs_t + 标准 action_t
        ↓
写入 HDF5
```

训练关系才是：

```text
选定的 obs_t → BC → 预测 action_t
                     ↓
          与标准 action_t 比较误差
```

这种“老师生成答案时知道得更多，学生只能看指定 observation 模仿”的情况，在模仿学习中很常见。

---

# 补充课 3：collect、训练配置和 checkpoint 三句话

使用一个具体数据仓库来理解。

## 1. collect 决定 HDF5 提供哪些数据

当前 collect 的代码中写了一份 `OBS_KEYS` 清单，决定保存：

```text
图像
左手位置
左手姿态
左夹爪状态
右手位置
右手姿态
右夹爪状态
```

它还保存 actions 和 MuJoCo states。

因此 collect 相当于决定：

> 数据仓库里有哪些货物可供以后选择。

但 HDF5 不是“包含仿真中一切可能信息”的无限完整记录：

- `states` 比较接近完整的 MuJoCo 存档；
- `obs` 仍然只是 collect 明确选择保存的 observation；
- 某些专家使用的中间信息不一定单独保存在 `obs/` 中。

## 2. 训练配置决定 BC 使用哪些数据

假设 HDF5 有：

```text
A：图像
B：左手状态
C：右手状态
D：夹爪状态
```

训练配置可以选择：

```text
方案 1：A+B+C+D
方案 2：B+C+D
```

即使数据仓库有 A，方案 2 也不会把 A 输入模型。

当前 checkpoint 的实际训练配置选择了：

```text
图像 + 六项低维状态
```

也就是 collect 保存的七项 observation 当前全部被选用。

## 3. checkpoint 记录训练后的模型需要哪些数据

训练完成保存 checkpoint 时，不只保存神经网络权重，还保存：

- 使用的算法和网络配置；
- observation key 清单；
- 每项 observation 的 shape；
- 是否使用图像；
- 相关预处理和环境元数据。

checkpoint 相当于模型毕业时携带的：

```text
学到的能力（网络权重）
+
输入说明书（需要哪些 observation）
```

在线运行时：

```text
读取 checkpoint 输入说明书
→ 从当前环境取出相同名字、相同 shape 的 observation
→ 做相同类型的预处理
→ 输入模型
→ 得到 action
```

如果训练时使用图像，调用时却不给图像，模型不能正常工作。  
如果训练时图像是指定尺寸、格式和相机视角，调用时随意换掉，也可能不报结构错误但表现明显下降。

## 4. 三句话的最终版本

```text
collect 决定：HDF5 中可以选择哪些数据

训练配置决定：训练这一次 BC 时实际选择哪些数据

checkpoint 记录：这个训练好的 BC 以后要求调用者提供哪些数据
```

## 5. 本轮理解检查

假设：

```text
HDF5 有：图像、手臂状态、夹爪状态
训练配置只选：手臂状态、夹爪状态
```

回答：

1. 这个 BC 训练时会学习图像吗？
2. 这个 checkpoint 在线调用时必须提供图像吗？
3. 如果另一次训练把图像也选上，新 checkpoint 的输入要求会不会改变？

## 6. 学习者回答与纠正

学习者回答的原则：

> 是否使用图像要看训练配置是否提供和选择图像。

原则正确。但本例已经明确给出：

```text
训练配置只选：手臂状态、夹爪状态
```

因此本例不是“不一定”，而是能够确定：

1. 这个 BC 不会学习图像。
2. 这个 checkpoint 在线调用时不要求提供图像。
3. 如果另一次训练把图像选上，新 checkpoint 的输入要求会改变，并要求图像。

需要形成的判断顺序：

```text
先看问题是否已经给出训练配置
├── 已给出 → 根据配置直接确定
└── 未给出 → 才回答“不一定，需要看配置”
```

本知识点的核心理解已经建立，下一轮只做一次最终确认后进入模型训练过程。

最终确认回答：

1. 当前 checkpoint 在线运行必须提供图像。
2. 缺少模型要求的图像会报错。
3. HDF5 即使保存深度图，只要训练配置没有选择，当前 BC 就不会使用。

判断：三项全部正确，第 5 课通过。

---

# 第 6 课：BC 模型的一次训练更新

本课只讲一个 batch 怎样让模型改变一点，不讲完整训练命令。

## 1. 从 HDF5 取一批训练样本

当前 checkpoint 配置的 batch size 是 32。

可以理解成一次从 HDF5 中取 32 组训练样本：

```text
(obs_1, 标准 action_1)
(obs_2, 标准 action_2)
...
(obs_32, 标准 action_32)
```

不是看完一条 demonstration 才更新一次，而是训练程序不断抽取小批数据进行更新。

## 2. 模型先做一次预测

对每个 obs：

```text
obs → BC 网络 → 预测 action
```

刚开始模型参数还没学好，预测通常和 collect 标准 action 差很多。

例如只抽象看一个动作数值：

```text
标准 action = 0.80
模型预测     = 0.20
```

## 3. 计算 loss

loss 是“预测答案与标准答案差多少”的数字。

当前 checkpoint 配置中：

```text
l2_weight = 1.0
l1_weight = 0.0
cos_weight = 0.0
```

因此当前训练主要使用 L2 / MSE：

```text
MSE = 平均值[(预测 action - 标准 action)²]
```

上面的单数值例子：

```text
(0.20 - 0.80)² = 0.36
```

实际 action 有很多维，而且一个 batch 有多条样本，所以程序会综合计算平均误差。

## 4. 反向传播更新模型

程序根据 loss 判断：

> 模型中的哪些参数向哪个方向调整，可以让下一次预测误差更小？

随后优化器把网络参数修改一点。

当前配置使用：

```text
AdamW
初始学习率 0.0001
```

学习率可以先理解成：

> 每次纠错时允许模型参数改变多大一步。

## 5. 重复很多次

```text
取一个 batch
→ BC 预测 actions
→ 与标准 actions 比较
→ 计算 loss
→ 反向传播
→ 更新参数
→ 再取下一个 batch
```

不断重复后，理想情况是：

```text
BC(obs) 越来越接近 collect_action
```

## 6. epoch、batch、checkpoint

- **batch**：一次拿来计算和更新的一小批样本，当前为 32。
- **epoch**：训练程序完成一个训练轮次；具体遍历方式还受每轮步数配置影响。
- **checkpoint**：训练到某个阶段保存下来的模型权重和输入说明书。

当前配置的总训练轮数设置为 500，并且配置了定期保存 checkpoint。

## 7. 一个重要边界

loss 变小只代表：

```text
模型更会模仿训练数据中的 action
```

不自动等于：

```text
模型在比赛中一定能稳定抓住所有箱子
```

如果在线 observation 与训练数据差异很大，例如：

- 机器人站位改变；
- yaw 改变；
- 相机画面改变；
- 箱子位置改变；
- 光照或遮挡改变；

模型可能进入没学过的情况，预测误差和抓取失败率都会升高。

## 8. 最短记忆

```text
obs → BC → 预测 action
             ↕ 比较
        collect 标准 action
             ↓
            loss
             ↓
          更新 BC 参数
```

## 9. 理解检查

1. loss 比较的是 BC 预测 action 和谁？
2. loss 变小代表模型更会模仿训练数据，是否能直接证明比赛抓取一定成功？
3. 学习率控制的是每次参数更新的步子大小，还是 HDF5 保存的数据量？

## 10. 学习者回答

1. loss 比较 BC 预测 action 和 HDF5 中对应 obs 的 collect 标准 action。
2. loss 变小不能直接证明比赛抓取一定成功，只表示更接近训练数据中的标准 action。
3. 学习率控制参数每次更新的步子大小。

判断：三项全部正确，第 6 课通过。

术语修正：

```text
action：动作命令
attention：注意力机制
```

本处应使用 `action`。

教学管理更新：

- 已建立可调整的全项目教学路线。
- 后续按“单一概念 → 代码事实 → 小例子 → 理解检查”的节奏推进。
- 根据学习者问题动态插课、合并或回退，不机械追求固定课数。

---

# 第 7 课：HDF5 树结构与 shape

## 1. 当前证据边界

当前仓库中没有发现由 L1 collect 脚本实际生成的抓取 HDF5。  
因此本课分为：

- collector 代码已经明确规定的结构；
- checkpoint 已经明确规定的模型输入 shape；
- 只有实际采集后才能确认的 `T`、demo 数量和 state 维度。

## 2. HDF5 树结构

成功 demonstration 写成：

```text
data/
├── demo_1/
│   ├── states
│   ├── actions
│   └── obs/
│       ├── robot0_left_eef_pos
│       ├── robot0_left_eef_quat
│       ├── robot0_left_gripper_qpos
│       ├── robot0_right_eef_pos
│       ├── robot0_right_eef_quat
│       ├── robot0_right_gripper_qpos
│       └── robot0_robotview_image
├── demo_2/
│   └── ...
└── attributes
```

每个 `demo_N` 是一次成功 rollout。

## 3. T 是时间步数量

假设一条 demonstration 执行了 350 次 action：

```text
T = 350
```

这条 demonstration 就有：

```text
350 个 obs
350 个 actions
350 个对齐后的 states
```

代码会检查：

```text
len(obs[key]) == len(actions)
len(states) == len(actions)
```

不同 demonstration 的 `T` 可以不同，因为稳定修正阶段可能提前达到目标，也可能执行更多步。

## 4. 当前 checkpoint 的低维 shape

已从 checkpoint 的 `shape_metadata` 确认：

| observation | 单个时间步 shape | HDF5 一条 demo 的时间序列 |
| --- | --- | --- |
| left_eef_pos | `(3,)` | `(T, 3)` |
| left_eef_quat | `(4,)` | `(T, 4)` |
| left_gripper_qpos | `(6,)` | `(T, 6)` |
| right_eef_pos | `(3,)` | `(T, 3)` |
| right_eef_quat | `(4,)` | `(T, 4)` |
| right_gripper_qpos | `(6,)` | `(T, 6)` |

含义：

- `(3,)`：三个位置数字，通常为 x、y、z。
- `(4,)`：四元数姿态的四个数字。
- `(6,)`：该夹爪的六个关节位置观测。

shape 中的数字表示一个时间步包含多少个数，不表示时间步数量。  
最前面的 `T` 才是整条轨迹的时间长度。

## 5. 图像 shape

checkpoint 要求处理后的图像 shape：

```text
(3, 128, 128)
```

含义：

```text
3   = RGB 三个颜色通道
128 = 图像高度
128 = 图像宽度
```

一条 demonstration 的图像序列可理解为：

```text
(T, 3, 128, 128)
```

采集环境原始图像通常以高度、宽度、通道顺序产生；robomimic 在送入网络前会转换成模型要求的通道优先形式。当前 checkpoint 规格以送入模型的 `(3,128,128)` 为准。

## 6. action shape

checkpoint 的：

```text
ac_dim = 20
```

表示每个时间步的 action 是 20 个控制数字：

```text
action_t.shape = (20,)
```

一条长度为 T 的 demonstration：

```text
actions.shape = (T, 20)
```

这些数字组合控制左右臂、夹爪以及 Tiago 复合控制器中的其他部件。当前阶段只要求知道它不是“向左移动”这样的文字，而是长度为 20 的数值向量。

## 7. states shape

可以确定：

```text
states.shape = (T, S)
```

其中：

- `T`：这条 demonstration 的时间步数；
- `S`：MuJoCo 完整 flattened state 的长度。

当前没有实际生成的数据集，因此本课不虚构 `S` 的具体数值。实际采集后可以直接读取。

## 8. 一个完整例子

假设：

```text
demo_1 的 T = 350
```

则：

```text
left_eef_pos         (350, 3)
left_eef_quat        (350, 4)
left_gripper_qpos    (350, 6)
right_eef_pos        (350, 3)
right_eef_quat       (350, 4)
right_gripper_qpos   (350, 6)
robotview_image      350 张 128×128 RGB 图像
actions              (350, 20)
states               (350, S)
```

## 9. 最短记忆

```text
T = 一条示范有多少个时间步
3 = 一个位置有三个数
4 = 一个四元数姿态有四个数
6 = 一侧夹爪观测有六个关节数
20 = 一个完整 action 有二十个控制数
```

## 10. 理解检查

1. `actions.shape = (350, 20)` 中，350 和 20 分别表示什么？
2. `left_eef_pos.shape = (350, 3)` 中，3 表示三条 demonstration，还是一个时间步的位置有三个数？
3. `states.shape = (T, S)` 中，为什么现在不能直接说出 S？

## 11. 学习者回答与时间步补充

已理解：

- `20` 表示每个时间步 action 中有 20 个控制数字。
- `3` 表示一个末端位置有 x、y、z 三个数。
- 当前目录没有 collect 实际生成的抓取 HDF5，因此不能直接读取真实 T 和 S。

待澄清：时间步。

### 什么是一个时间步

在这个采集循环里，一次：

```text
env.step(action)
```

就是一个控制时间步。

完整的一步：

```text
读取 obs_t
→ collect 产生 action_t
→ env.step(action_t)
→ MuJoCo 向前模拟一个控制周期
→ 得到新的状态
```

然后进入下一个时间步。

例如：

```text
t=0：
看到初始画面和手臂状态
→ action_0：双臂向上
→ MuJoCo 执行

t=1：
看到双臂已经升高一点
→ action_1：继续向上
→ MuJoCo 执行

t=2：
看到双臂又升高一点
→ action_2：继续调整
→ MuJoCo 执行
```

因此时间步不是一次完整抓取，而是完整抓取中的一个很小动作周期。

### 20 Hz 的含义

collect 环境设置：

```text
control_freq = 20 Hz
```

即仿真每秒接收约 20 次控制 action：

```text
1 个时间步 ≈ 1 / 20 秒 = 0.05 秒仿真控制时间
```

如果一条轨迹有：

```text
T = 350
```

名义控制时长约为：

```text
350 / 20 = 17.5 秒
```

这是仿真控制时间，不一定等于电脑实际运行耗时；渲染和计算可能让现实等待时间更长。

### 类比

视频由很多帧组成，一次抓取由很多控制时间步组成：

```text
视频：frame_0 → frame_1 → frame_2 → ...
抓取：step_0  → step_1  → step_2  → ...
```

但每个控制时间步不仅有画面，还包含：

```text
obs_t + action_t + state_t
```

### 当前暂停确认

1. 一次完整抓取通常包含一个时间步，还是很多时间步？
2. 在 20 Hz 控制频率下，20 个时间步大约对应多少秒仿真控制时间？
3. `T=350` 表示350次成功抓取，还是一条抓取轨迹中有350个小动作周期？

## 12. 学习者回答与 HDF5 保存位置

学习者已正确理解：

- 一次完整抓取包含很多时间步。
- 20 Hz 下，20 个时间步约为 1 秒仿真控制时间。
- `T=350` 表示一次抓取中记录350组 observation，并对应350个 actions，不是350次抓取。

### 最终 HDF5 默认保存位置

collect 参数默认：

```text
directory = robosuite/robosuite/models/assets/demonstrations_private
output_name = factory_sorting_grasp
```

运行时再创建一个精确到分钟的时间戳目录：

```text
demonstrations_private/
└── YYYYMMDDHHMM/
    └── factory_sorting_grasp_YYYYMMDDHHMM.hdf5
```

例如：

```text
JCIIOT/
└── robosuite/robosuite/models/assets/demonstrations_private/
    └── 202607281630/
        └── factory_sorting_grasp_202607281630.hdf5
```

当前项目中的默认目录已经存在，并有一个：

```text
202607280930/
```

时间戳目录，但检查时其中没有实际 HDF5 文件。因此目前仍没有可直接分析的 L1 抓取数据集。

### 原始临时数据不是最终 HDF5

collect 运行时还会创建：

```text
factory_sorting_grasp_raw_...
```

临时目录，里面先保存：

- episode 目录；
- `model.xml`；
- `ep_meta.json`；
- `state_*.npz`。

运行全部 rollout 后，脚本把成功 episode 汇总为最终 HDF5。

所以是两处：

```text
系统临时目录：
原始 episode / NPZ

项目 demonstrations_private/时间戳目录：
最终 HDF5
```

### 350 个 obs 的准确说法

可以说：

> 一次抓取有350个时间步，因此保存350组 observation。

每一组 observation 内又包含七项：

```text
左手位置
左手姿态
左夹爪状态
右手位置
右手姿态
右夹爪状态
机器人相机图像
```

对应：

```text
350组 obs
350个 action
```

### 理解检查

1. 最终 HDF5 默认保存在系统临时目录，还是项目的 `demonstrations_private/时间戳目录`？
2. `T=350` 时，是350张图像还是350组 observation？两种说法有什么关系？

## 13. 学习者回答

- 最终 HDF5 保存在 `demonstrations_private/时间戳目录`。
- observation 包含位置、姿态、夹爪状态和图像，因此 `T=350` 表示350组 obs；每组又有一张图像，所以同时有350张图像。

判断：正确，第 7 课通过。

### 是否需要关心临时目录

正常训练：

```text
主要使用最终 HDF5
```

通常不需要手动处理临时 episode。

以下情况需要查看临时目录：

- 最终 HDF5 没有成功 demo；
- obs/action 长度转换时报错；
- 想检查某次失败 rollout；
- 想用 `model.xml` 和 actions 复现原始过程。

脚本会打印临时目录路径，但不会主动删除。多次采集后应注意磁盘占用。

---

# 第 8 课：怎样判断一份 HDF5 可以训练

“成功生成 `.hdf5` 文件”不等于“里面有可用训练数据”。

## 1. 第一项：成功 demo 数必须大于 0

文件属性中有：

```text
num_successful_demos
```

如果：

```text
num_successful_demos = 0
```

即使磁盘上存在 HDF5 文件，也没有可供 BC 学习的成功 demonstration。

## 2. 第二项：每条 demo 的 T 必须合理

检查：

```text
actions.shape[0]
obs[key].shape[0]
states.shape[0]
```

它们都应该表示同一条轨迹的时间步数 T。

collector 已自动检查：

```text
每项 obs 长度 == actions 长度
states 长度 == actions 长度
```

但仍要观察 T 是否异常：

- T 太小：可能动作流程提前结束；
- T 极大：可能机械臂长时间无法到达或数据重复；
- 不同 demo 差异异常大：可能有不稳定阶段。

## 3. 第三项：shape 要与训练配置相容

当前 checkpoint 规格例如：

```text
left_eef_pos       (T, 3)
left_eef_quat      (T, 4)
left_gripper_qpos  (T, 6)
robotview_image    每步 128×128 RGB
actions            (T, 20)
```

如果新数据的 action 是其他维度，而训练环境和配置要求20维，训练或推理会失败。

## 4. 第四项：数值不能损坏

低维数组和 actions 中不应出现：

```text
NaN
Infinity
```

否则 loss 可能变成 NaN，模型无法正常学习。

还应检查 action 是否几乎全为0。  
如果绝大多数 action 都是0，模型可能主要学会“不动”。

## 5. 第五项：图像要真的有效

文件中有 image dataset 不代表画面一定正确。

要抽样检查：

- 是否全黑；
- 是否上下颠倒；
- 是否来自 `robot0_robotview`；
- 是否能看见机器人和目标物；
- 是否每帧完全相同；
- 尺寸是否正确。

## 6. 第六项：成功标签是否可信

当前 collector 只要求双臂夹爪通过 grasp check 并保持，没有执行抬升验证。

因此即使 demo 被标为成功，也应该抽样回放确认：

- 是否真的夹在合理位置；
- 是否只是碰撞检查短暂成立；
- 闭爪后物体是否稳定；
- 这种动作能否支持在线后续 lift。

数据标签如果不可靠，模型会认真模仿错误示范。

## 7. 自动检查与人工检查

collector 已自动做：

- 只保存 successful episode；
- obs/action 长度一致；
- states/action 长度一致；
- 缺少 observation key 时直接报错。

仍建议额外做：

- demo 数量和 T 分布；
- shape；
- NaN / Infinity；
- action 分布；
- 图像抽样；
- demonstration 回放；
- grasp 后 lift 可行性。

## 8. 最短记忆

一份可训练 HDF5 至少要满足：

```text
有成功 demo
长度对齐
shape 正确
数值正常
图像有效
成功标签可信
```

## 9. 理解检查

1. HDF5 文件存在，但 `num_successful_demos=0`，能否正常训练出抓取策略？
2. obs 和 actions 的 T 不一致，这份数据是否可以直接训练？
3. 图像 dataset 存在，但所有画面全黑，为什么仍然属于坏数据？

## 10. 学习者回答

1. 不能。
2. 不能。
3. 模型无法从全黑图像中学习目标和机械臂的位置关系。

判断：三项全部正确，第 8 课通过。

---

# 第 9 课：训练配置是训练过程的“配方”

训练配置不是 HDF5，也不是模型权重。它是一份说明：

```text
从哪里取数据
一次取多少数据
把哪些 observation 输入模型
使用什么网络和损失
训练多久
把 checkpoint 保存到哪里
```

## 1. data：训练数据路径

当前 checkpoint 内嵌配置记录的原训练数据是：

```text
robomimic/datasets/202606222228_low_dim.hdf5
```

检查当前仓库后，该文件不存在。

这意味着：

- 现有 checkpoint 可以继续用于在线推理；
- 但不能仅凭这个路径直接重新读取原始训练数据；
- 如果要重新训练，需要使用自己 collect 生成的 HDF5，并更新配置中的 data path。

注意文件名含 `low_dim`，但 checkpoint 配置明确同时选择了 RGB 图像。文件名不能代替实际配置事实。

## 2. batch_size：一次取多少样本

当前配置：

```text
batch_size = 32
```

每次参数更新使用一批样本，而不是一次只用一个，也不是一次把整个 HDF5 全部塞入模型。

## 3. num_epochs 和每轮步数

当前配置：

```text
num_epochs = 500
epoch_every_n_steps = 100
```

可先理解为：

- 计划执行500个训练轮次；
- 每轮执行100个训练 batch step。

因此配置层面最多约：

```text
500 × 100 = 50,000 个 batch step
```

实际是否完整训练到结束，还要看训练是否中断、是否恢复和具体日志，不能只靠文件名判断。

## 4. modalities：模型选择哪些 observation

当前配置选择：

```text
low_dim:
  左右手位置
  左右手姿态
  左右夹爪状态

rgb:
  robot0_robotview_image
```

这就是前面所说“训练配置决定模型实际使用哪些 HDF5 数据”的具体位置。

## 5. algo：使用哪一种 BC 网络

当前配置：

```text
algo_name = bc
transformer.enabled = true
```

因此它不是最简单的单帧全连接 BC，而是启用了 Transformer 的 BC policy。

当前阶段只记住：

```text
算法类别：Behavior Cloning
策略网络：启用 Transformer
```

时间上下文下一课单独讲。

## 6. loss 和 optimizer

当前配置：

```text
主要 loss：MSE / L2
optimizer：AdamW
initial learning rate：0.0001
```

这些决定如何比较预测 action，以及怎样更新模型参数。

## 7. output_dir 和保存频率

当前配置记录：

```text
output_dir = ../bc_transformer_trained_models
every_n_epochs = 50
```

表示训练产物放到指定输出目录，并计划每50个 epoch 定期保存一次 checkpoint。

实际最终路径还与运行时工作目录和实验名有关，后续讲训练命令时再解析。

## 8. config、HDF5、checkpoint 的位置关系

```text
HDF5：
训练材料

config：
训练配方

训练程序：
按照配方处理材料

checkpoint：
训练后的成品模型 + 输入说明
```

## 9. 当前复现状态

已有：

- robomimic 训练代码；
- 现有 checkpoint；
- checkpoint 内嵌训练配置；
- collect 脚本。

缺少：

- checkpoint 原来使用的 `202606222228_low_dim.hdf5`；
- 仓库中可直接复用的这次训练外部配置 JSON；
- 当前 collect 生成的有效 HDF5。

因此之后真正重新训练前，需要：

```text
collect 新数据
→ 验证 HDF5
→ 准备外部训练 config
→ 把 data path 指向新 HDF5
→ 启动训练
```

## 10. 理解检查

1. HDF5、训练配置、checkpoint 分别更像“材料、配方、成品”中的哪一个？
2. 原 checkpoint 记录的数据路径已经不存在，现有 checkpoint 还能不能用于在线抓取？
3. 如果用自己新生成的 HDF5 训练，需要修改配置中的哪一项？

## 11. 学习者回答

- HDF5 是材料。
- 训练配置是配方。
- checkpoint 是成品。
- 原 HDF5 不存在时，已有 checkpoint 仍可用于在线抓取。
- 使用新 HDF5 训练时，要修改 `data path`。

判断：全部正确，第 9 课通过。

---

# 第 10 课：真正执行训练的是哪个文件

## 1. 训练程序入口

当前仓库中真正负责启动 robomimic 训练的是：

```text
JCIIOT/robomimic/scripts/train.py
```

它主要做：

1. 读取外部 config JSON；
2. 检查 HDF5 路径是否存在；
3. 从 HDF5 读取环境和 shape metadata；
4. 创建 BC 网络、优化器和数据加载器；
5. 逐 epoch 训练；
6. 保存日志、视频和 `.pth` checkpoint。

## 2. 启动训练需要四类文件/路径

### A. 训练程序

```text
robomimic/scripts/train.py
```

作用：执行训练流程。

### B. 数据集

```text
factory_sorting_grasp_....hdf5
```

作用：提供 obs、标准 actions 和环境 metadata。

### C. 训练配置

例如未来需要准备：

```text
factory_sorting_bc.json
```

作用：指定 modalities、网络、batch、epoch、学习率、保存目录等。

当前仓库只有通用 BC / BC Transformer 模板，没有找到与现有 FactorySorting checkpoint 完全对应、可直接运行的外部配置 JSON。checkpoint 内部保存了配置内容，但训练入口的首选方式是读取外部 JSON。

### D. 输出 checkpoint

```text
model_epoch_....pth
last.pth
```

作用：供在线 `pick_up` 加载。

## 3. 命令结构

从 `JCIIOT/` 目录运行时，基本结构是：

```text
python robomimic/scripts/train.py
  --config <训练配置.json>
  --dataset <抓取数据.hdf5>
  --name <实验名称>
```

其中：

- `--config`：加载外部训练配方；
- `--dataset`：覆盖 config 中原来的 HDF5 路径；
- `--name`：设置本次实验名。

当前没有准备好匹配 FactorySorting 的外部 config 和有效 HDF5，因此现在只学习命令结构，不实际启动训练。

## 4. train.py 如何检查数据

训练开始时会逐个检查：

```text
config.train.data 中的 path 是否存在
```

路径不存在时直接报：

```text
Dataset at provided path ... not found
```

所以仅有 `train.py` 不能训练，必须同时提供实际 HDF5。

## 5. 输出目录结构

训练程序根据：

```text
config.train.output_dir
config.experiment.name
当前时间戳
```

创建：

```text
output_dir/
└── experiment_name/
    └── YYYYMMDDHHMMSS/
        ├── logs/
        ├── models/
        ├── videos/
        ├── last.pth
        └── last_bak.pth
```

其中：

- `logs/`：训练日志；
- `models/`：定期 checkpoint；
- `videos/`：rollout 视频；
- `last.pth`：最近模型；
- `last_bak.pth`：最近模型备份。

## 6. 通用模板不是现成比赛配置

仓库中存在：

```text
robomimic/config/default_templates/bc_transformer.json
```

但它只是通用模板。不能只改 dataset path 就假设一定与当前 FactorySorting 数据匹配，还必须检查：

- observation keys；
- 图像输入；
- action dimension；
- frame stack；
- Transformer 配置；
- 环境 metadata；
- 输出目录。

## 7. 四个角色的最终对应

```text
train.py     = 厨师 / 训练执行程序
HDF5         = 材料
config JSON  = 配方
checkpoint   = 成品
```

## 8. 理解检查

1. 真正执行训练循环的是 HDF5、config，还是 `train.py`？
2. 只有 `train.py` 和 config、没有 HDF5，能否开始训练？
3. 训练完成后，供 `pick_up` 加载的是 `.json` 配置还是 `.pth` checkpoint？

## 9. 学习者回答与文件指向规则

学习者回答：

1. `train.py` 执行训练循环。
2. 没有 HDF5 不能训练。
3. 训练后给 `pick_up` 使用 `.pth` checkpoint。

判断：全部正确，第 10 课通过。

新增教学规则：

> 后续每当讲到脚本、配置、数据集、checkpoint 或输出目录，必须主动给出对应项目文件路径，并明确标记“存在”“不存在”或“运行后生成”。

---

# 第 10 课补充：训练配方在哪里，输出到哪里

## 1. “训练配方”具体是什么

训练配方就是 config JSON，主要包含：

```text
algo_name：
使用 BC、BCQ 还是其他算法

train：
HDF5 路径、batch size、epoch、CUDA、输出目录

observation.modalities：
使用哪些 low_dim、RGB、depth observation

algo：
Transformer、loss、optimizer、learning rate

experiment：
实验名、保存频率、日志、rollout 和视频设置
```

训练入口读取 config 后，根据这些字段创建完整训练过程。

## 2. 当前模型的精确配方存在哪里

当前模型：

```text
JCIIOT/robosuite/robosuite/model_epoch_150.pth
```

这个 `.pth` 内部有一个：

```text
config
```

字段，保存了训练该模型时的完整配置。

重要现状：

- checkpoint 内嵌配置：存在；
- 与该模型完全对应的独立外部 config JSON：仓库中未找到；
- 原始训练 HDF5：仓库中未找到。

因此当前精确训练配方“在 checkpoint 里面”，而不是一个可以直接点击编辑的独立 JSON。

## 3. 仓库中现有的通用模板

存在两个 BC Transformer 通用模板：

```text
JCIIOT/robomimic/config/default_templates/bc_transformer.json
JCIIOT/robomimic/exps/templates/bc_transformer.json
```

它们是通用起点，不是当前 FactorySorting 模型的精确原始配方。

未来真正重新训练前，建议从 checkpoint 配置和模板整理出一个明确的比赛配置，例如：

```text
JCIIOT/robomimic/config/factory_sorting_bc_transformer.json
```

注意：该建议文件目前不存在，后续实操训练阶段再生成并验证。

## 4. 当前配置的输出目录怎样解析

checkpoint 配置记录：

```text
output_dir = ../bc_transformer_trained_models
experiment.name = test
```

robomimic 将相对 `output_dir` 按 robomimic 包目录解析。

得到基础绝对路径：

```text
D:\project\JCIIOT2026-master\JCIIOT2026-master\JCIIOT\bc_transformer_trained_models
```

按当前实验名和时间戳，运行后会形成：

```text
JCIIOT/bc_transformer_trained_models/
└── test/
    └── YYYYMMDDHHMMSS/
        ├── logs/
        ├── models/
        ├── videos/
        ├── last.pth
        └── last_bak.pth
```

当前检查结果：

```text
JCIIOT/bc_transformer_trained_models/
```

尚不存在，说明仓库中没有保留按该输出设置产生的完整训练运行目录。

已有的：

```text
JCIIOT/robosuite/robosuite/model_epoch_150.pth
```

是被单独放到 robosuite 包目录供在线抓取使用的 checkpoint。

## 5. 文件路径索引

| 用途 | 路径 | 当前状态 |
| --- | --- | --- |
| 训练程序 | `JCIIOT/robomimic/scripts/train.py` | 存在 |
| 通用 Transformer 配置 | `JCIIOT/robomimic/config/default_templates/bc_transformer.json` | 存在 |
| 另一通用实验模板 | `JCIIOT/robomimic/exps/templates/bc_transformer.json` | 存在 |
| FactorySorting 专用外部配置 | `JCIIOT/robomimic/config/factory_sorting_bc_transformer.json` | 不存在，后续准备 |
| 原训练 HDF5 | `JCIIOT/robomimic/datasets/202606222228_low_dim.hdf5` | 不存在 |
| 现有 checkpoint | `JCIIOT/robosuite/robosuite/model_epoch_150.pth` | 存在 |
| 默认训练输出根目录 | `JCIIOT/bc_transformer_trained_models/` | 当前不存在，训练后生成 |

## 6. 理解检查

1. 当前模型的精确训练配方是在独立 JSON 中，还是内嵌在 `.pth` checkpoint 中？
2. 仓库里的 `bc_transformer.json` 是 FactorySorting 的精确原配方，还是通用模板？
3. 训练运行后生成的 checkpoint 默认应先出现在 `models/`，还是 `demonstrations_private/`？

## 7. 学习者回答与目录纠正

学习者回答：

1. 精确配置内嵌在 `.pth`：正确。
2. `bc_transformer.json` 是通用模板：正确。
3. 新 checkpoint 出现在 `demonstrations_private/`：不正确。

正确目录职责：

```text
demonstrations_private/
→ 存 HDF5 demonstration
→ 是训练材料

bc_transformer_trained_models/.../models/
→ 存训练生成的 .pth checkpoint
→ 是模型成品
```

训练完成后，如果要让在线 `pick_up` 使用新模型，有两种方式：

1. 把新 `.pth` 放到计划使用的位置，再修改 `knowledge/robot_params.json` 的 `grasp_policy.checkpoint_path`；
2. 不移动 `.pth`，直接把 `checkpoint_path` 指向训练输出中的模型路径。

不要求 checkpoint 必须放进 `demonstrations_private/`，该目录不应用于保存模型成品。

### 相关文件路径

| 内容 | 路径 |
| --- | --- |
| HDF5 默认目录 | `JCIIOT/robosuite/robosuite/models/assets/demonstrations_private/` |
| 训练输出模型目录 | `JCIIOT/bc_transformer_trained_models/<实验名>/<时间戳>/models/` |
| 在线模型路径配置 | `JCIIOT/knowledge/robot_params.json` |

### 最终确认

填空：

```text
HDF5 → ________ 目录
.pth → ________ 目录
```

## 8. 学习者最终确认

```text
HDF5 → demonstrations_private
.pth → models
```

判断：正确，目录职责知识点通过。

---

# 第 11 课：当前 BC 模型内部的数据流水线

当前模型不是“把图像和坐标直接变成 action”的一个黑盒块。可以分成四段：

```text
图像编码
低维状态整理
多时间步 Transformer
20维 action 输出
```

## 1. 图像编码

输入：

```text
robot0_robotview_image
shape = (3, 128, 128)
```

当前 checkpoint 配置使用：

```text
ResNet18Conv
→ SpatialSoftmax
→ 64维视觉特征
```

直观理解：

- ResNet18Conv 从像素中提取边缘、形状、物体和空间相关特征；
- SpatialSoftmax 更强调“重要视觉特征大概位于图像哪里”；
- 最终把大张图像压缩成更短的视觉特征向量。

模型后面处理的是提取后的特征，不是逐像素直接决定20维 action。

## 2. 低维状态整理

同一个时间步还有：

```text
左手位置       3
左手姿态       4
左夹爪状态     6
右手位置       3
右手姿态       4
右夹爪状态     6
```

共：

```text
3 + 4 + 6 + 3 + 4 + 6 = 26 个低维数值
```

这些状态不需要经过图像卷积，而是作为低维特征送入 observation encoder。

## 3. 图像特征和低维状态融合

一个时间步的模型信息可抽象为：

```text
64维视觉特征
+
26维手臂/夹爪状态
→ 当前时间步的综合 observation 特征
```

具体网络会把不同模态编码后组合，并映射到 Transformer 使用的 embedding 空间。

当前 Transformer：

```text
embed_dim = 128
```

可以先理解为每个时间步最终被整理成模型内部的一份128维表示。

## 4. Transformer 看多个时间步

当前配置：

```text
context_length = 10
num_layers = 2
num_heads = 4
```

它不是只看当前一帧，而是看最近10个时间步的 observation 序列：

```text
obs_(t-9)
obs_(t-8)
...
obs_t
```

Transformer 用 attention 判断不同历史时刻对当前动作的重要程度。

这里：

```text
attention = 注意力机制
action = 最终动作命令
```

二者不是同一个词。

## 5. 输出20维 action

Transformer policy 为各时间步生成 action 表示。当前训练设置只监督序列最后一个时间步的 action。

最终：

```text
最近10步 observation
→ Transformer
→ 当前预测 action
→ shape = (20,)
```

输出还经过 `tanh`，把动作值限制在：

```text
[-1, 1]
```

随后这个20维 action 交给 MuJoCo 环境执行。

## 6. 完整数据流

```text
最近10步机器人图像
→ ResNet18Conv + SpatialSoftmax
→ 视觉特征
                         \
                          → 每步综合特征
                         /
最近10步手臂和夹爪状态
→ 低维编码

每步综合特征序列
→ 2层、4头 Transformer
→ 当前20维 action
→ tanh 限制到 [-1,1]
→ MuJoCo
```

## 7. 相关文件路径

| 内容 | 文件 |
| --- | --- |
| BC Transformer 算法封装、batch 和 loss | `JCIIOT/robomimic/algo/bc.py` |
| Transformer action policy | `JCIIOT/robomimic/models/policy_nets.py` |
| 多模态 observation encoder | `JCIIOT/robomimic/models/obs_nets.py` |
| ResNet18Conv、SpatialSoftmax 等基础网络 | `JCIIOT/robomimic/models/base_nets.py` |
| 当前模型及内嵌网络配置 | `JCIIOT/robosuite/robosuite/model_epoch_150.pth` |

## 8. 理解检查

1. 当前模型是只看一张当前图像，还是看最近10个时间步的 observation？
2. `attention` 和 `action` 分别是什么？
3. 模型最终输出的 action 有多少维，数值被限制在哪个范围？

## 9. 学习者回答

- 当前模型看最近10个时间步的 observation。
- attention 是 Transformer 注意力机制。
- action 是动作命令。
- action 为20维，范围 `[-1, 1]`。

判断：全部正确，第 11 课通过。

---

# 第 12 课：10步历史从哪里来

当前 checkpoint 同时记录：

```text
frame_stack = 10
transformer.context_length = 10
pad_frame_stack = true
```

## 1. 训练时怎样取10步

HDF5 本身按时间顺序保存：

```text
obs_0
obs_1
obs_2
...
```

训练数据加载器在取某个时刻 `t` 时，会连同前面的历史一起取出：

```text
obs_(t-9) ... obs_t
```

形成长度为10的输入窗口，再交给 BC Transformer。

所以 HDF5 不需要在每一行重复存10份历史；历史窗口由数据加载器从连续时间序列中组装。

## 2. 轨迹刚开始时历史不足怎么办

在 `t=0` 时，只有第一组 observation，没有真实的前9步。

当前配置：

```text
pad_frame_stack = true
```

做法是重复初始 observation：

```text
t=0 输入：
[obs_0, obs_0, obs_0, obs_0, obs_0,
 obs_0, obs_0, obs_0, obs_0, obs_0]
```

到下一步：

```text
t=1 输入：
[obs_0, obs_0, obs_0, obs_0, obs_0,
 obs_0, obs_0, obs_0, obs_0, obs_1]
```

随着真实历史增多，重复项逐渐被替换。

## 3. 在线运行怎样保持10步

在线环境使用：

```text
FrameStackWrapper
```

它为每个 observation key 维护一个最多保存10项的队列。

每执行一次 action：

```text
MuJoCo 产生新 obs
→ 新 obs 放到队列末尾
→ 最旧 obs 从队列开头移除
→ 得到最新10步窗口
```

例如窗口已经装满：

```text
原窗口：
[obs_0, obs_1, ..., obs_9]

执行 action 后得到 obs_10：
[obs_1, obs_2, ..., obs_10]
```

这叫滑动窗口。

## 4. 为什么训练和在线必须一致

训练时模型学的是：

```text
最近10步 obs → 当前 action
```

如果在线只提供一帧：

- 输入 shape 不符合模型要求；
- 模型失去运动趋势信息；
- 通常会报错或无法按训练方式运行。

因此在线 `make_eval_env()` 会读取 checkpoint config，并按照 `frame_stack=10` 包装环境。

## 5. 10步代表多长时间

控制频率为20 Hz：

```text
10 / 20 = 0.5秒
```

所以当前模型每次预测 action 时，大约参考最近0.5秒的 observation 历史。

它可以借此分辨：

- 手臂正在接近还是远离；
- 夹爪正在张开还是闭合；
- 箱子在画面中的相对变化；
- 前几步 action 后状态如何变化。

## 6. attention 在这里的作用

Transformer 接收10步历史后，用 attention 学习：

> 当前输出 action 时，10个历史时刻中哪些信息更重要。

它并不是固定地平均10步，也不是人为规定第几帧最重要；重要性由训练学习。

## 7. 相关文件路径

| 内容 | 文件 |
| --- | --- |
| 训练时组装历史窗口 | `JCIIOT/robomimic/utils/dataset.py` |
| 根据 config 给在线环境添加 frame stack | `JCIIOT/robomimic/utils/env_utils.py` |
| 在线维护10步队列和起始重复填充 | `JCIIOT/robomimic/envs/wrappers.py` |
| BC Transformer 检查 context length | `JCIIOT/robomimic/algo/bc.py` |
| 当前 frame stack 配置 | `JCIIOT/robosuite/robosuite/model_epoch_150.pth` 内嵌 config |

## 8. 理解检查

1. `t=0` 时没有前9步历史，当前配置怎样补足10步？
2. 在线得到 `obs_10` 后，窗口 `[obs_0 ... obs_9]` 会变成什么？
3. 20 Hz 下，10步历史大约覆盖多少秒？

## 9. 学习者回答

- 起始历史不足时重复初始 observation。
- 新 observation 到来后使用滑动窗口，移除最旧项并加入最新项。
- 20 Hz 下10步约覆盖0.5秒。

判断：全部正确，第 12 课通过。

---

# 第 13 课：怎么看 BC 训练得好不好

“训练了很多 epoch”不等于“训练得好”。至少要区分三个指标：

```text
training loss
validation loss
rollout success rate
```

## 1. training loss

训练 loss 衡量：

```text
BC 在训练 batch 上预测的 action
与
HDF5 标准 action
之间的误差
```

当前主要使用 MSE。

训练 loss 下降说明：

> 模型越来越能模仿训练数据。

但它只说明对见过的训练分布拟合得更好。

## 2. validation loss

验证 loss 应该在未参与参数更新的 demonstration 上计算。

作用：

> 检查模型对没直接拿来训练的数据，是否也能预测得好。

典型过拟合：

```text
training loss 持续下降
validation loss 反而上升
```

表示模型越来越会背训练数据，但对新数据表现变差。

当前 checkpoint 配置：

```text
experiment.validate = false
```

说明这次训练没有启用独立 validation loss。

因此不能从该 checkpoint 配置中获得“验证集表现很好”的证据。

## 3. rollout success rate

rollout 是把当前模型真正放进 MuJoCo 环境执行完整抓取，再统计成功率。

例如：

```text
测试50次
成功40次
rollout success rate = 40 / 50 = 80%
```

它比 training loss 更接近比赛目标，因为模型的 action 会影响下一步 observation：

```text
预测 action
→ 环境变化
→ 新 observation
→ 再预测
```

一次小错误可能不断累积，最终导致抓取失败。单步 MSE 不一定能完整反映这种累积。

## 4. 当前 checkpoint 的 rollout 配置

当前内嵌配置：

```text
rollout.enabled = true
rollout.n = 50
rollout.horizon = 400
rollout.rate = 50
terminate_on_success = true
```

可理解为：

- 每次 rollout 评估运行50个 episode；
- 每个 episode 最多400步；
- 每50个 epoch 进行一次 rollout 评估；
- 成功后可以提前结束该 episode。

保存设置：

```text
every_n_epochs = 50
on_best_rollout_success_rate = true
```

即定期保存，并在 rollout 成功率刷新最好成绩时保存 checkpoint。

## 5. “训练充分”应该怎么看

不能只看：

```text
epoch 很多
training loss 很低
```

更可靠的判断至少包括：

1. 数据中有足够多且可信的成功 demonstration；
2. train loss 正常下降，没有 NaN；
3. 有独立 validation 或不同初始条件测试；
4. rollout success rate 在多次评估中稳定；
5. 在线抓取的站位、yaw、相机和 observation 与训练一致；
6. grasp 后 lift 也真正成功，而不只是单步 action 接近。

## 6. 当前模型证据的边界

checkpoint 配置能证明：

- 训练设置了500 epoch；
- 定期进行了 rollout 的设计；
- 配置要求根据最佳 rollout success rate 保存。

但仅凭 checkpoint 配置不能证明：

- 实际完整跑完500 epoch；
- 最终 success rate 是多少；
- 原始50次 rollout 的统计结果；
- 在比赛当前代码和场景中仍保持同样成功率。

因为原训练日志、视频和完整输出目录当前不在仓库中。

## 7. 最短记忆

```text
train loss：
会不会模仿训练答案

validation loss：
对未训练样本能否保持预测能力

rollout success：
放进环境后能否真正完成抓取
```

## 8. 相关文件路径

| 内容 | 文件 |
| --- | --- |
| BC loss 计算 | `JCIIOT/robomimic/algo/bc.py` |
| 训练、验证、rollout 和保存流程 | `JCIIOT/robomimic/scripts/train.py` |
| rollout 统计与保存条件 | `JCIIOT/robomimic/utils/train_utils.py` |
| 当前验证和 rollout 配置 | `JCIIOT/robosuite/robosuite/model_epoch_150.pth` 内嵌 config |
| 原训练日志目录 | 当前仓库中不存在 |

## 9. 理解检查

1. training loss 很低，能否直接证明 MuJoCo 抓取成功率很高？
2. training loss 下降但 validation loss 上升，可能说明什么？
3. 50次 rollout 成功40次，success rate 是多少？

## 10. 学习者回答

- training loss 很低不能直接证明 MuJoCo 抓取成功率很高。
- train loss 下降但 validation loss 上升，可能发生过拟合。
- `40 / 50 = 80%`。

判断：全部正确，第 13 课通过。

---

# 第 14 课：数据覆盖决定模型能应对哪些情况

## 1. 当前 collect 默认20次是否代表20种情况

当前 collector 默认：

```text
num_rollouts = 20
object = line_5_container_h01_near
robot_base_pos = [8.000001, 4.600000, 0.0]
robot_base_yaw = 3.139422
```

目标箱子在环境中使用固定 pose，机器人底盘位姿也固定。

因此，默认运行20次更接近：

```text
在几乎相同的初始条件下重复20次
```

不等于：

```text
20种不同站位、角度、箱子位置和视角
```

物理模拟可能产生细小差别，但代码没有主动对目标箱子或底盘位姿做范围随机化。

## 2. 为什么重复相同成功示范仍有用

它可以帮助模型学习：

- 标准站位下的接近轨迹；
- 双臂如何移动；
- 什么时候闭爪；
- 标准视角下箱子和夹爪的关系。

如果比赛在线执行几乎完全复现相同条件，这类数据可能有效。

## 3. 为什么覆盖太窄会失败

训练数据都来自：

```text
x = 8.0
y = 4.6
yaw ≈ 3.139
固定箱子位置
```

但在线如果变成：

```text
x = 8.10
y = 4.52
yaw = 3.05
```

相机画面、手臂相对箱子位置都会改变。

模型可能进入训练数据没有覆盖的 observation，称为：

```text
distribution shift
分布偏移
```

即训练时看到的情况与使用时看到的情况不同。

## 4. 小误差为什么会累积

专家 collect 每一步都能读取精确 grasp site，再修正方向。

BC 没有直接读取 grasp site，只根据 observation 模仿。

如果 BC 某一步偏了一点：

```text
预测 action 偏差
→ 手臂进入训练数据很少出现的位置
→ 下一步 observation 更陌生
→ 下一个 action 可能偏差更大
```

这种现象叫误差累积。

因此：

```text
单步 MSE 很小
```

仍不保证完整350步轨迹稳定。

## 5. 怎样增加合理的数据覆盖

未来可以在安全范围内采集：

- 底盘 x、y 小范围偏移；
- yaw 小范围偏移；
- 目标箱子位置小范围偏移；
- 机械臂初始状态小范围变化；
- 不同但有效的成功接近轨迹。

但不能盲目大范围随机：

- grasp site 可能超出机械臂可达范围；
- 专家脚本会失败；
- 图像和动作关系过于复杂；
- 成功数据比例下降。

正确方向是：

```text
围绕比赛中真实可能出现的误差范围做受控随机化
```

## 6. 当前已有的图像增强

checkpoint 配置中有：

```text
CropRandomizer
crop_height = 76
crop_width = 76
```

训练时会对图像做随机裁剪，增加一定视觉变化。

但随机裁剪不能替代真实物理覆盖：

```text
图像裁剪变化
≠
机器人真的换了站位和yaw
```

## 7. 当前证据边界

可以确认：

- 当前仓库的 collect 默认位姿和目标物固定；
- 当前 checkpoint 使用图像随机裁剪；
- checkpoint 原始训练 HDF5 不在仓库。

因此不能确认：

- 原 checkpoint 的数据是否只有20条；
- 原数据是否做过其他位姿随机化；
- 原训练集实际覆盖范围。

不能把当前 collector 默认值直接当成现有 checkpoint 原始数据集的全部事实。

## 8. “训练充分”的更准确说法

训练是否充分至少包含：

```text
数据数量
数据质量
数据覆盖范围
训练是否收敛
是否过拟合
rollout成功率
训练与在线输入是否一致
```

不是只增加 epoch 数。

## 9. 相关文件路径

| 内容 | 文件 |
| --- | --- |
| 默认20次、固定底盘位姿和目标物 | `JCIIOT/robosuite/robosuite/environments/factory_sorting/load_factory_sorting_1_3fo3erfhisem_collect.py` |
| 目标箱子固定 pose | `JCIIOT/robosuite/robosuite/environments/factory_sorting/factory_sorting_1_3fo3erfhisem.py` |
| 训练抓取位姿配置 | `JCIIOT/knowledge/task_config.json` |
| checkpoint 路径和推理参数 | `JCIIOT/knowledge/robot_params.json` |
| 当前图像 CropRandomizer 配置 | `JCIIOT/robosuite/robosuite/model_epoch_150.pth` 内嵌 config |
| 原训练 HDF5 | 当前仓库中不存在 |

## 10. 理解检查

1. 固定相同站位采集20次，是否等于覆盖20种不同抓取情况？
2. BC 某一步偏离训练轨迹后，为什么后面的 action 可能越来越不准？
3. 图像随机裁剪能否完全替代底盘位置和 yaw 的真实变化？

---

# 2026-07-29 复习记录

## 第14课回答

1. 用户回答“不一定”：相同起点和终点下，过程可能存在差异。补充结论：过程可能有微小变化，但这不等于主动覆盖了20种具有代表性的抓取条件；固定初始条件与专家规则通常会产生高度相似的轨迹。
2. 回答正确：前一步偏离后产生了训练数据中少见的 `obs`，模型难以给出准确 `action`，误差可能继续累积。
3. 回答正确：随机图像裁剪不能替代底盘位置、yaw 和物体位姿的真实物理变化。

## 本次复习安排

1. 总流程与角色分工。
2. HDF5 与 BC 训练链路。
3. checkpoint、在线推理和训练效果。

## 第一轮复习结果：总流程与角色分工

1. `collect` 是专家老师，BC 是模仿学习的学生——正确。
2. `obs` 是当前状态与视角信息，`action` 是机器人在该 `obs` 下采取的动作；两者必须按同一时间步对应——正确。
3. 比赛在线抓取时由 BC 模型输出动作，不再运行 `collect` 专家——正确。
4. scene JSON 主要服务于 `move` 导航——正确。
5. 当前 `place_down` 没有使用抓取 BC 模型——正确。

## 第二轮复习结果：HDF5 与时间步

1. 350个时间步不是350次抓取，而是一次抓取执行过程中连续记录的350组数据——正确。更严格地说，是350次连续的“观察、动作、物理更新”，不是预先把动作平均切成350份。
2. 控制频率20Hz时，100个时间步约为5秒——正确。
3. 同一时间步的 `obs_7` 应与专家 `action_7` 对应——正确。
4. HDF5保存图像不等于BC一定使用图像，取决于训练配置——正确。
5. `states`不一定作为BC输入，同样取决于训练配置；当前模型主要使用配置选定的 `obs`——正确。

## 第三轮复习结果：训练配置、checkpoint 与在线输入

1. HDF5中有图像，但训练配置未选图像，则训练出的BC不需要图像——正确。
2. 训练时使用图像，比赛时不提供图像，不能正常工作——正确。
3. checkpoint不保存完整原始HDF5——正确。
4. 20Hz下的10步历史约为0.5秒——正确。
5. 与训练loss相比，40/50的rollout结果更直接反映连续执行任务的成功能力——正确。

## 第四轮复习补充：抓取点的局部坐标与世界坐标

- 抓取点在环境构建阶段作为箱子物体内部的 MuJoCo `site` 加入，因此代码中写入的 `pos` 是相对箱子坐标系的局部坐标。
- 对 `line_5_container_h01_near`，环境代码定义：
  - 右抓取点：`[0.29, +0.120, top_z]`
  - 左抓取点：`[0.29, -0.120, top_z]`
- 环境把这些局部坐标加入 `container.get_obj()`；MuJoCo结合箱子的当前世界位置与姿态，持续计算每个site的世界坐标。
- `collect`通过 `env.sim.data.site_xpos[site_id]`读取的是当前世界坐标，不是原始局部坐标。
- `collect`再用“抓取点世界坐标－夹爪世界坐标”得到世界坐标系中的位置误差，并转换到控制器坐标系，形成专家动作。
- 到达抓取点不等于抓取成功；成功仍需两臂各自通过 `_check_grasp` 接触检查。

## 第四轮复习结果

1. `[0.29, 0.120, top_z]`是相对于箱子的局部坐标——正确。局部点随箱子一起运动，MuJoCo可据此更新世界坐标。
2. `sim.data.site_xpos`提供当前世界坐标——正确。
3. 箱子旋转或移动后，抓取点世界坐标需要随之改变——正确。
4. 当前BC不能像`collect`一样从普通 `obs` 直接取得精确抓取点世界坐标——正确。

关键理解：BC从大量 `obs → action` 对中逐步学会靠近合适抓取位置。不过，BC通常不是显式预测一个名为“抓取点世界坐标”的中间结果，而是把这种位置关系隐含在模型参数中，直接从观察历史预测20维动作。

## 显式抓取点与隐式学习：检查结果

1. 不能从BC的20维动作中直接读出抓取点世界坐标——正确。
2. BC训练时将预测动作与 `collect` 专家动作比较——正确。
3. 对“箱子移动到训练数据没有覆盖的位置后谁更容易继续找到抓取点”，用户初答BC；需纠正为 `collect`。`collect`可以每次从MuJoCo读取更新后的精确 `site_xpos`，BC只能依靠有限训练分布中学到的 `obs → action` 关系，超出分布后不保证泛化。

纠正后的确认结果：

1. 箱子移动后，`collect`读取更新后的抓取点世界坐标——正确。
2. BC只在固定位置训练过，换位置后不能保证成功——正确。
3. 提高位置泛化能力，应采集多个合理位置的成功示范，而不只是重复同一位置——正确。

---

# collect 源码补充：从 rollout 到训练入口

## 第一部分：创建环境与一次 rollout 的初始化

主程序首先解析参数并准备环境配置，然后调用 `suite.make(...)` 创建真实的 FactorySorting MuJoCo 环境。这里创建的是三维仿真环境，不是读取scene JSON进行抓取。

随后用 `DataCollectionWrapper` 包裹环境：

```text
raw_env：真正执行物理仿真的环境
DataCollectionWrapper：在env.step时顺便记录state和action
```

包装器的原始轨迹先写入系统临时目录 `factory_sorting_grasp_raw_*`。最终HDF5输出目录和名称同时准备好，但此时还没有把demo汇总到最终HDF5。

主循环对每个rollout调用 `rollout_once(...)`。每条rollout开头执行：

1. `env.reset()`：把仿真恢复到这一条示范的初始状态。
2. 取得底层环境、机器人与目标物名称。
3. 建立空的 `obs_buffer`。
4. 根据目标物上的site名称读取当前抓取点世界坐标。
5. 读取左右机械臂末端起始世界坐标。
6. 据此准备安全高度、水平接近位置和最终抓取位置。

关键点：`collect`不负责把局部抓取点手动转换成世界坐标；它通过 `site_xpos`读取MuJoCo已转换好的世界坐标。

另一个关键点：执行阶段不是直接逐步写最终HDF5。当前观察先放入内存 `obs_buffer`；包装器把state/action写入临时episode目录；所有rollout结束后，脚本才筛选成功episode并合并成最终HDF5。

## 补充二：专家如何从目标位置生成一个 action

`collect` 专家生成动作时，并不是把 `obs_buffer` 中的图像或普通观测输入一个模型。它拥有仿真环境的“教师特权”，可以直接读取 MuJoCo 中的精确位置。

每个控制时间步的主要过程如下：

1. 更新机器人控制器当前状态。
2. 分别读取左右臂末端执行器的当前世界坐标。
3. 用“目标点世界坐标 - 末端当前世界坐标”得到 `world_delta`。
4. 如果控制器使用机器人底座坐标系，把世界坐标差转换到控制器坐标系。
5. 根据控制器允许的位移范围把位置差归一化，并用 `max_action` 限幅。
6. 把左右臂位移动作、夹爪开合值、躯干、头部和底座动作拼成机器人完整 action。
7. 在执行 action 之前，记录当前 `obs_t`。
8. 调用 `env.step(action_t)`，由 MuJoCo 计算运动、碰撞、接触和新的物理状态。
9. 下一控制时间步再次读取更新后的状态，并生成新的 `action_(t+1)`。

因此一条训练数据的对应关系是：

```text
MuJoCo精确状态 ──专家规则──> action_t
       │
       └──同时生成并保存──> obs_t

HDF5训练对：(obs_t, action_t)
```

关键区分：

- 专家 `collect` 生成 `action_t` 时，可以直接使用 MuJoCo 精确坐标。
- BC 训练时看不到专家内部使用的计算过程，只能从训练配置选中的 `obs_t` 学着预测 `action_t`。
- `env.step(action_t)` 产生的是更新后的状态；它会在下一个时间步形成新的观测，而不是与刚执行的 `action_t` 错位配对。

## 补充三：一整条抓取示范与在线 pick_up 的分工

### collect 的一条 rollout

一条专家抓取示范依次执行：

1. `env.reset()`，初始化环境、机器人、目标物和空的 `obs_buffer`。
2. 读取目标箱子左右抓取 site 的当前世界坐标。
3. 空夹爪保持打开，先竖直抬到安全高度，避免途中碰到物体。
4. 在安全高度进行 XY 水平移动，到达抓取点正上方。
5. 空夹爪竖直下降到抓取点附近。
6. 微调夹爪末端中心，使左右夹爪分别对准左右抓取目标。
7. 连续多个时间步执行闭爪动作。
8. 检查左右夹爪是否都建立了有效抓取。
9. 两侧都成功后继续闭爪保持若干步，并把 episode 标记为成功。

其中代码中的 `safe vertical lift` 是抓取前空机械臂的安全抬升，不是抓住箱子后的举升。

### 成功示范筛选

失败 rollout 虽然在执行过程中也会产生临时 state、action 和内存中的 obs，但主流程只把成功 rollout 的 `obs_buffer` 放入 `obs_cache`。合并最终 HDF5 时，还会再次检查临时 episode 的 `successful` 标志；失败 episode 被跳过。

因此最终训练 HDF5 只保留被 collector 判定为成功的示范。

### collector 与在线 pick_up 的区别

当前 collector 的成功条件是“双臂完成抓取并保持”，它本身没有在闭爪后执行带箱子的向上举升。

比赛在线 `pick_up` 的完整流程分为两个部分：

```text
BC抓取策略
  → 接近、闭爪、判断抓取

独立的 lift_after_grasp 规则控制器
  → 保持闭爪并把物体向上举升

最终 pick_up 成功
  = grasp_success 且 lift_success
```

所以当前系统不是让同一个 BC 模型同时学习“抓取”和“抬升”。BC checkpoint 负责抓取阶段；抓住后的 lift 由独立程序控制。

## 补充四：成功示范如何进入训练

### 最终 HDF5 的生成

collector 所有 rollout 结束后，`gather_successful_demonstrations_as_hdf5(...)` 会遍历临时 episode：

1. 读取临时 `state_*.npz` 中的 states、actions 和 successful 标志。
2. 跳过没有 state 或 `successful=False` 的 episode。
3. 为每条成功示范创建 `data/demo_1`、`data/demo_2` 等分组。
4. 在每个 demo 中写入：
   - `states`
   - `actions`
   - `obs/<各个观测键>`
   - `model_file`
5. 检查每个 obs 序列长度是否与 action 数量相同。
6. 在 HDF5 的 `data` 分组属性中写入环境名称、环境参数、采集参数和成功示范数量。

图像观测使用 gzip 压缩；低维观测直接保存。

默认输出位置和命名为：

```text
robosuite/robosuite/models/assets/demonstrations_private/
└── YYYYMMDDHHMM/
    └── factory_sorting_grasp_YYYYMMDDHHMM.hdf5
```

因为当前 collector 已经同时写入 obs、actions、states 和 robomimic 环境元数据，所以它输出的 HDF5 可以直接作为训练数据，不需要再从 states 生成一次 obs。

### 训练入口

项目提供的通用 robomimic 训练入口是：

```text
JCIIOT/robomimic/scripts/train.py
```

标准调用形式：

```text
python robomimic/scripts/train.py --config <训练配置.json> --dataset <成功示范.hdf5>
```

其中：

- `--config` 决定算法、使用哪些 obs、网络结构、历史窗口、batch size、epoch、CUDA、保存规则和输出目录。
- `--dataset` 可以覆盖配置文件中的 `train.data` 数据路径。
- 训练程序读取 HDF5 环境元数据和观测形状，创建数据集与模型，然后按 epoch 训练。
- checkpoint 输出位置由 `config.train.output_dir`、`config.experiment.name`、训练时间戳共同决定，模型保存在其 `models/` 子目录。
- 每个 `.pth` checkpoint 会保存模型参数，同时嵌入训练 config、环境元数据和观测形状等推理所需信息。

仓库提供的是通用 `train.py`，而不是一个单独封装好的 L1 专用训练脚本。真正开始重新训练前，需要准备与目标 BC 输入严格一致的训练配置 JSON。

---

# 第7课：真实 HDF5 树结构与 shape

## 当前数据文件检查

当前仓库中没有可读取的 FactorySorting 抓取 HDF5：

- `robosuite/robosuite/models/assets/demonstrations_private/202607280930/` 为空目录。
- `robosuite/dataset/table_setup_from_dishwasher_sample.hdf5` 只有 134 字节，内容是 Git LFS 指针，不是真实 HDF5；指针声明的原文件大小约为 591 MB。
- 当前 checkpoint 内嵌的原训练数据路径 `robomimic/datasets/202606222228_low_dim.hdf5` 也不在仓库中。

因此本课暂时不能声称已经测量了真实 demonstration 的 `T` 和 HDF5 dataset shape。先使用 collector 代码与 checkpoint metadata 确认预期结构，等实际生成成功 HDF5 后再做最终验收。

## checkpoint 已确认的单时间步输入 shape

`model_epoch_150.pth` 的 `shape_metadata` 明确记录：

| 输入键 | 单时间步 shape |
| --- | --- |
| `robot0_left_eef_pos` | `(3,)` |
| `robot0_left_eef_quat` | `(4,)` |
| `robot0_left_gripper_qpos` | `(6,)` |
| `robot0_right_eef_pos` | `(3,)` |
| `robot0_right_eef_quat` | `(4,)` |
| `robot0_right_gripper_qpos` | `(6,)` |
| `robot0_robotview_image` | `(3, 128, 128)` |
| action | `(20,)` |

六项低维 observation 合计为：

```text
3 + 4 + 6 + 3 + 4 + 6 = 26维
```

当前 checkpoint：

- 使用以上全部六项低维 observation；
- 使用 `robot0_robotview_image` RGB 图像；
- `use_images=True`；
- 不使用 depth；
- `frame_stack=10`；
- `seq_length=1`；
- `batch_size=32`；
- 原训练配置为 500 epochs。

## 时间维 T

checkpoint 的 shape metadata 描述的是一个时间步送入模型前的单帧 shape，不包含 HDF5 demonstration 的时间维。

如果一条 demo 有 `T=350`：

```text
actions                              (350, 20)
obs/robot0_left_eef_pos              (350, 3)
obs/robot0_left_eef_quat             (350, 4)
obs/robot0_left_gripper_qpos         (350, 6)
obs/robot0_right_eef_pos             (350, 3)
obs/robot0_right_eef_quat            (350, 4)
obs/robot0_right_gripper_qpos        (350, 6)
```

collector 直接记录的图像通常是通道在最后的 `(T, 128, 128, 3)`；robomimic 预处理后，checkpoint 看到的单时间步图像是通道在前的 `(3, 128, 128)`。由于当前没有真实采集 HDF5，原始图像 dataset 的 shape 仍需在实际文件生成后读取确认。

`demo` 数量和时间步数量是两层概念：

```text
data/demo_1：T1个时间步
data/demo_2：T2个时间步
...

成功示范数 = demo分组数量
总训练时间步 = T1 + T2 + ...
```

---

# 第8课：数据有效性检查

拿到 HDF5 后，不应直接开始训练。固定按以下顺序验收。

## 1. 文件真实性

- 文件能否被 `h5py.File(..., "r")` 打开；
- 是否只是 Git LFS 指针；
- 文件大小是否合理；
- 是否存在 `data` 分组和至少一个 `demo_N`。

路径存在不等于它是真实 HDF5。当前仓库中 134 字节的 `.hdf5` 就是 Git LFS 指针，不能训练。

## 2. 结构与长度

检查每个 demo：

- `actions`、`states`、`obs` 是否存在；
- 所需 observation keys 是否齐全；
- `len(actions)` 是否等于每个 obs 的时间维；
- shape 是否与训练配置和 checkpoint 预期一致；
- `data.attrs["env_args"]` 等环境元数据是否存在并可解析。

collector 在写最终文件时已经检查 obs/action 长度，并删除多出的最后一个 state，使 states 与 actions 对齐；但拿到外部数据后仍要重新验收。

## 3. 数值有效性

检查：

- 是否出现 NaN 或 Inf；
- action 是否在允许范围内，robomimic 的检查脚本要求全局位于 `[-1, 1]`；
- action 的 min、max、mean、std；
- 是否大量动作长期固定为零；
- 是否大量动作顶在上限，形成严重饱和。

某些 action 维度固定为零可能是设计结果，例如 collector 当前不控制旋转或底座；但本应移动的手臂位置维度若始终为零，则数据可疑。

## 4. 图像有效性

不能只检查 shape，还应抽样确认：

- 图像不是全黑、全白或完全相同；
- dtype 和数值范围合理；
- 相机内容确实包含机器人和目标物；
- HDF5 图像方向与在线推理预处理后的方向一致；
- 不存在训练图像倒置、在线图像正常的分布不一致。

## 5. demonstration 数量与分布

检查：

- 成功 demo 数量；
- 每条轨迹长度的 min、max、mean、std；
- 是否所有 demo 几乎完全重复；
- 是否覆盖计划中的位置、yaw、物体和视角变化；
- 是否只有成功数据造成恢复状态缺失。

成功筛选能去掉错误答案，但也会产生偏差：BC 只看到成功轨迹，较少看到“已经偏离后如何恢复”。

## 6. 训练集与验证集

应按完整 demo 划分 train/valid，而不是把同一条轨迹的相邻时间步随机拆到两边，否则训练集和验证集高度相似，验证 loss 会虚假地好看。

项目提供：

```text
robomimic/scripts/split_train_val.py
```

默认验证比例为 10%。例如 20 条 demo 会分出约 2 条验证 demo。

## 项目自带摘要工具

```text
python robomimic/scripts/get_dataset_info.py --dataset <文件.hdf5> --verbose
```

它会报告：

- 总时间步；
- demo 数量；
- 轨迹长度统计；
- action 最小值和最大值；
- train/valid 等 filter keys；
- 环境元数据；
- demo 内各 dataset 的 shape。

这个工具适合第一轮摘要，但不会替代 NaN、图像内容、动作分布和轨迹多样性的深度检查。

## 第8课完成确认

- 134 字节的 `.hdf5` 是 Git LFS 指针，不能作为训练数据。
- 必须按完整 demo 划分训练集和验证集；否则同一轨迹的相邻时间步会泄漏到两边，使验证结果虚高。
- 如果 BC 配置使用图像，训练与在线推理的图像方向必须一致；如果配置完全不使用图像，图像翻转才不会影响该模型。

---

# 第9课：比赛所需的训练配置最小阅读

比赛准备中需要区分三种容易混淆的“配置”。

## 1. 训练模板

仓库中的通用 BC Transformer 模板：

```text
JCIIOT/robomimic/config/default_templates/bc_transformer.json
```

它提供可修改的初始配方，但不等于现有 checkpoint 当年实际使用的配方。例如模板当前填写 `batch_size=100`、`num_epochs=2000`，而已检查的 `model_epoch_150.pth` 内嵌配置为 `batch_size=32`、`num_epochs=500`。因此不能根据模板反推旧模型。

## 2. 实际训练配置

训练时通过 `--config` 指定的 JSON 才是本次训练真正使用的配方。比赛中优先检查：

- `train.data`：训练 HDF5；
- `observation.modalities.obs`：模型使用哪些 obs；
- `train.frame_stack`：一次决策使用多少帧历史；
- `train.seq_length`：训练样本的序列长度；
- `train.batch_size`、`train.num_epochs`：训练批量与轮数；
- `train.cuda`：是否请求 GPU；
- `train.output_dir`：训练产物根目录。

其中真正决定“旧 checkpoint 能否继续使用”的核心是输入输出契约：obs keys、各项 shape、frame stack 和 action dimension。batch size、epoch、学习率主要影响训练过程，不是在线模型的输入格式。

## 3. checkpoint 内嵌配置

`.pth` 不只保存模型权重，也保存实际训练配置和 shape metadata。在线加载 checkpoint 时，robomimic 根据其中的信息重建相同结构的 BC 模型。

因此：

- 模板表示“准备怎么训练”；
- checkpoint 内嵌配置表示“这个成品模型实际上需要什么”；
- 二者不一致时，以当前加载的 checkpoint 为准。

## 4. `robot_params.json` 不是训练配置

```text
JCIIOT/knowledge/robot_params.json
```

它是比赛运行阶段的执行参数。`grasp_policy.checkpoint_path` 决定加载哪个模型；`eval_steps` 决定 BC 最多运行多少步；`lift` 段控制抓住后的独立抬升。它不会改变 checkpoint 内部已经训练好的 obs keys、网络结构或 action dimension。

## 比赛中的最短判断顺序

1. 当前失败发生在 move、BC 抓取、lift 还是 place。
2. 若是 BC 抓取，确认运行时实际加载哪个 checkpoint。
3. 查看该 checkpoint 的输入契约是否与在线 obs 一致。
4. 若契约一致但场景变化导致抓取失败，再考虑补采数据和重训。
5. 若只是 lift、导航或放置失败，不要先去重训 BC。

## 第9课完成确认

- 修改 batch size 不会改变旧 checkpoint 的 action dimension。
- 旧 checkpoint 不能直接接收训练时不存在的新 observation key。
- 已经抓住、抬升时掉落，应先检查独立的 lift 阶段，而不是先重训 BC。

---

# 每道比赛题的固定开题流程

这是一条贯穿后续所有课程的比赛工作流，不单独占用课程编号。

课程顺序最终确定为：先完成整体比赛链路，再按 L1、L2、L3、L4、L5 逐题建立事实表、跑基线、定位首个失败阶段和制定准备方案。逐题阶段不会只讲 L1、L2。

## 1. 先制作“本题事实表”

只记录六项：

1. level / scene / env_name；
2. 材料的自然语言描述；
3. source 的人类站名和程序 ID；
4. target 的人类站名和程序 ID；
5. 精确 object_name 与数量；
6. 抓取停止位姿，以及当前使用的 checkpoint。

信息来源按角色区分：

- 官方 DOCX 顶部当前任务 Prompt：本题要搬什么、从哪到哪、数量多少；
- `knowledge/sopN.md`：代码转换和补充后的 LLM 知识；
- `knowledge/task_config.json`：level、scene、程序站点 ID、object_name、grasp pose 的锁定运行映射；
- 对应 `maps/*_scene_regenerated.json`：场景几何、站点和障碍物信息；
- `knowledge/robot_params.json`：当前 checkpoint 与 move、lift、place 执行参数。

不可把通用 SOP 正文中的示例名称直接当作本题变量。L2 官方 Word 顶部明确写的是 Green-rimmed storage bin、Pick Station 1、Place Station 3、数量 1，但正文仍出现 Pick Station 2 和 blue hollow plastic bin；这种矛盾应以顶部 Current Task Prompt 和锁定运行映射交叉确认。

## 2. 不改参数，先跑一次基线

记录：

- LLM 生成的四步是否为 `move → pick_up → move → place_down`；
- 实际 source、target 和 object_name 是否正确；
- 第一个失败阶段；
- 得分、耗时、终端信息和轨迹录像。

不要看到最终失败就直接重训。先找到第一个失败阶段。

## 3. 按第一个失败阶段进入对应入口

- 计划或名称错误：先看 `src/robot_agent/workflows/task_flow.py`、相关 skill 及知识读取结果；
- 到不了取货位：看 `skills/move.py`、语义地图、占据栅格和 `robot_params.json/navigation`；
- 到位但抓取失败：看抓取停止位姿、object_name、在线 obs、checkpoint 和训练覆盖；
- 已夹住但抬升失败：看 `robot_params.json/lift`；
- 运输失败：回到 move、障碍物和携物后的路径；
- 放置失败：看 `skills/place_down.py` 与 `robot_params.json/place`。

## 4. 单变量实验

一次只调整一个允许修改的变量，使用同一道题、同一初始条件重跑，并记录修改前后得分和第一个失败阶段。禁止为了“试试看”同时改导航、checkpoint、lift 和 place。

## L1 与 L2 的具体区别

- L1：`input_5 → output_4`，对象为蓝色空心箱；生成的 `sop1.md` 直接提供精确 BC 训练停止位姿。
- L2：`input_6 → output_4`，对象为绿色 tote；`sop2.md` 没有直接给精确抓取停止位姿，但锁定的 `task_config.json` 提供 `(6.0, 4.8, 0.0)`、yaw `-3.139453`。
- L2 官方 Word 的通用 SOP 正文含有 L1 遗留描述，因此必须区分顶部任务 Prompt 和通用正文。
- L2 增加了跨线运输和避障压力；即使仍使用同一 BC checkpoint，move 路径、物体外观和抓取数据覆盖也与 L1 不同。

## 如果 L1 checkpoint 在 L2～L5 都到站但抓取失败

这说明 LLM 四步规划和 move 已经基本通过，但只表示“到达了站点”，还要先核实是否到达每题所需的精确抓取位姿和 yaw。

若精确位姿、object_name、在线 obs 预处理和 checkpoint 输入契约都正确，而 BC 仍抓取失败，才可把主要原因定位为 L1 抓取数据对新物体、新视角或新场景覆盖不足，进入补采与重训。

当前仓库只提供：

```text
load_factory_sorting_1_3fo3erfhisem_collect.py
```

L2～L5 有对应 scene loader，但没有四个现成 collector。因此不能简单地分别运行四个已有脚本。新增采集方式必须在逐题阶段先确认比赛修改边界，再设计合规方案。

当前运行端 `robot_params.json` 只有一个全局 `grasp_policy.checkpoint_path`，不会按 level 自动切换：

- 若比赛每题预先已知并允许题间重启，可先训练分题模型，手动选择 checkpoint，便于快速验证每题是否可抓；
- 若一次运行要自动处理不同 level，应优先考虑把各题成功示范合并为统一训练集，训练一个覆盖 L1～L5 的统一 checkpoint；
- 也可以先用分题模型验证数据和物理流程，再合并数据训练统一模型。

## Word、VLM、Markdown 与 LLM 的实际关系

当前代码不是一条自动完成的：

```text
Word → VLM → 自动写 sopN.md → LLM
```

而是两条相连但彼此独立的路径。

文档读取路径：

```text
Word
→ ReadDocumentSkill 提取段落文字和内嵌图片
→ 可选 VLM 只负责描述图片
→ 返回 text 与 image_descriptions
```

当前 `read_document.py` 不会自动把结果保存成 `knowledge/sopN.md`。

规划路径：

```text
预先生成并已存在的 knowledge/*.md
→ KnowledgeManager 扫描和组织
→ planner 把知识块与当前场景 object mapping 注入提示词
→ LLM 输出结构化 JSON 计划
→ agent 依次调用 move、pick_up、move、place_down
```

因此，`sop1.md`～`sop5.md` 是当前已经准备好的锁定知识文件；`read_document` 是可被调用的文档读取 skill，不是现有 Markdown 知识库的自动生成器。


---
