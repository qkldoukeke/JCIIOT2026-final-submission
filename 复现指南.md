# 最终版复现说明

**代码仓库：** [https://github.com/qkldoukeke/JCIIOT2026-final-submission](https://github.com/qkldoukeke/JCIIOT2026-final-submission)

克隆后进入仓库的 `JCIIOT/` 目录执行以下安装和测试步骤。

## 1. 环境

- 推荐 Windows 10/11 + Miniconda/Anaconda + Python 3.11
- Python 3.12 符合项目声明，但部分固定版本科学计算依赖在不同平台上可能需要额外处理，因此最终复现优先使用 Python 3.11
- 克隆仓库前必须安装 Git LFS
- 项目依赖以 `requirements.txt`、根目录 `pyproject.toml` 和 `robosuite/pyproject.toml` 为准
- macOS 本地适配未写入提交源码；Linux/Windows 按主办方原始入口运行

## 2. 安装

Windows PowerShell 示例：

```powershell
git lfs install
git clone https://github.com/qkldoukeke/JCIIOT2026-final-submission.git
Set-Location .\JCIIOT2026-final-submission\JCIIOT
git lfs pull

conda create -n jciiot python=3.11 -y
conda activate jciiot

python -m pip install --upgrade pip setuptools wheel
python -m pip install -r requirements.txt
python -m pip install -e .\robosuite
python -m pip install -e .
```

如果仓库已经用普通 Git 克隆，但权重文件只有一百多字节，请在仓库根目录执行：

```powershell
git lfs pull
```

在 `JCIIOT` 目录执行：

```bash
python -m pip install --upgrade pip setuptools wheel
python -m pip install -r requirements.txt
python -m pip install -e ./robosuite
python -m pip install -e .
```

确认以下资源存在：

```text
robosuite/robosuite/model_epoch_150.pth
robosuite/robosuite/environments/factory_sorting/generated_maps/
team_submission/models/final/l1/model_epoch_20.pth
team_submission/models/final/l2/model_epoch_50.pth
team_submission/models/final/l3/model_epoch_100.pth
team_submission/models/final/l4/model_epoch_50.pth
team_submission/models/final/l5/model_epoch_100.pth
```

## 3. 启动与测试

主办方原始入口（推荐用于正式复现）：

```bash
python -m streamlit run app.py
```

Windows 也可以在已激活 `jciiot` 环境后运行便捷脚本：

```powershell
powershell -ExecutionPolicy Bypass -File .\team_submission\start_frontend.ps1
```

该脚本不包含开发电脑盘符，优先使用 `JCI_PYTHON`，其次使用当前 Conda 环境，再回退到系统 `PATH` 中的 Python。若本地 API 配置不存在，仍显示官方侧边栏供现场填写。

浏览器进入 Streamlit 提示的地址，配置可用的 LLM/VLM API，保持 **Inject Knowledge Base** 开启，然后依次点击 L1–L5 行的 **Execute**。无需手工更换模型或 SOP；当前环境会自动路由到对应生成 SOP 和 BC 恢复模型。

## 4. 离线审计

```bash
python team_submission/audits/verify_generated_sops.py
python -X utf8 team_submission/audits/verify_final_submission.py
python -X utf8=0 team_submission/audits/verify_final_submission.py
python team_submission/audits/validate_training_configs.py
```

官方边界审计需要指定主办方原始 ZIP：

```bash
python team_submission/audits/verify_official_boundary.py \
  --reference-zip /path/to/JCIIOT2026-80d8f9216b0716c6c7a20c19582b532ff1c9cdf2.zip
```

验证脚本分别输出 Python/JSON、模型、证据、模型路由、生成 SOP、受保护边界和文档一致性七组结果。若模型仍为百余字节的 Git LFS 指针，脚本会在计算模型哈希前提示执行 `git lfs install` 和 `git lfs pull`。

## 5. API 密钥

仓库不包含 API Key。请使用平台侧边栏或环境变量配置，不要把本机 `team_submission/.local/api_credentials.json` 上传到 GitHub。

## 6. 固定官方审计基准

- 主办方仓库：`https://github.com/JCIIOT2026/JCIIOT2026`
- 固定提交：`80d8f9216b0716c6c7a20c19582b532ff1c9cdf2`
- 官方 ZIP SHA-256：`d5245cb57b0c9c99253f397950a30a4255e98af78390cc2d0850784cb84bda2a`

固定提交中的官方模型、示例 HDF5 和四个桌面网格 ZIP 均已通过 Git LFS 正确实体化。它们不是从指针文本手工伪造的文件。

## 7. Windows 复现边界

五题历史满分记录来自 Windows 项目运行。提交版保留相同的最终 skill、参数和五个模型，并新增了按环境自动选择模型的路由；该路由已通过五环境映射、模型哈希和配置验证。正式提交前仍建议在一台干净 Windows 电脑上至少完成一次 L1–L5 顺序执行，最终成绩以主办方复现为准。

28 份训练配置规定从 `JCIIOT/` 根目录启动并使用项目相对路径。训练 HDF5 没有随 GitHub 仓库提交；推理复现不依赖这些数据，从头训练则必须另行取得 `team_submission/TRAINING_DATA_MANIFEST.md` 中列出的 HDF5 并校验 SHA-256。

## 8. 合规解释边界

主办方禁止修改的源码已通过固定基准审计；skill 内程序化抓取对仿真公开辅助接口的使用方式，仍待主办方对接口边界进行书面确认。当前版本保留满分运行行为，不宣称这一接口解释风险已经消除。
